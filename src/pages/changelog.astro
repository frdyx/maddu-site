---
import Layout from '../layouts/Layout.astro';
import Callout from '../components/Callout.astro';
import TocIndex from '../components/TocIndex.astro';
import PageNext from '../components/PageNext.astro';

type ReleaseType = 'milestone' | 'feature' | 'hardening' | 'polish' | 'patch';

interface Release {
  version: string;
  id: string;
  date: string;
  type: ReleaseType;
  headline: string;
  /** Curated bullet highlights (older, hand-authored entries). */
  highlights?: string[];
  /** Single-paragraph summary (backfilled entries mirrored from CHANGELOG.md). */
  summary?: string;
  /** Optional context / "why this matters" */
  note?: string;
}

const RELEASES: Release[] = [
  {
    version: 'v1.92.2',
    id: 'v1-92-2',
    date: '2026-07-04',
    type: 'patch',
    headline: 'focus director: verbosity is not drift — the absolute-anchor metric (incident catch)',
    summary: 'The drift tagger divided goal-token overlap by the focus text’s size, so every detailed, honest, on-goal slice summary read as away (5/5 false positives on one day) while a terse "working on autonomy" would have passed. The new metric counts absolute anchors — whether the goal’s distinctive terms appear at all: ≥2 anchors → toward, 1 → lateral, 0 → away — with light stemming, and the goal axis narrows to objective + constraints. The regression cases are the incident itself; every tag now carries its anchors, so it’s self-explanatory.',
  },
  {
    version: 'v1.92.1',
    id: 'v1-92-1',
    date: '2026-07-03',
    type: 'polish',
    headline: 'Cockpit surfaces earned autonomy',
    summary: 'AUTONOMY_SCORED / AUTONOMY_RECOMMENDATION events classify into the trust family with human summaries — a scored digest and rung-change arrows (backend: observe → relaxation-candidate) — and a live recommendation renders as its own Autonomy row on the orientation brief card, rebuilt from the spine via a new autonomy projection slot.',
  },
  {
    version: 'v1.92.0',
    id: 'v1-92-0',
    date: '2026-07-03',
    type: 'feature',
    headline: 'Earned autonomy — maddu autonomy (recommend-only)',
    summary: 'maddu autonomy — a deterministic per-lane trust score over the verified record: a Wilson lower bound over witnessed-clean vs witnessed-dirty slice outcomes, a 3-rung ladder (observe / established / relaxation-candidate), and a daily clean-credit cap against deliverable-farming. Recommend-only by contract: nothing in the feature writes governance config — applying a recommendation is the operator running maddu governance set. Recommendations are muted while any phase is active; maddu orient and governance show announce the latest one next to the tier it informs.',
  },
  {
    version: 'v1.91.2',
    id: 'v1-91-2',
    date: '2026-07-03',
    type: 'hardening',
    headline: 'doctor watches the global binary (incident catch)',
    summary: 'A stale npm i -g maddu on PATH shadows a newer checkout and silently runs old behavior — surfaced live when a stale global demanded --session inside a current checkout while doctor stayed green. doctor now compares the running CLI’s version against the checkout’s version.json (older → WARN with the npm i -g remedy), and the consumer framework-version WARN is direction-aware instead of advising maddu upgrade both ways.',
  },
  {
    version: 'v1.91.1',
    id: 'v1-91-1',
    date: '2026-07-03',
    type: 'polish',
    headline: 'Cockpit surfacing for the governance-arc domains',
    summary: 'The event stream renders COMPACTION_CHECKPOINT, vendor-memory facts, and the sterile-phase tier; the memory filter gains correction + vendor kinds; and the orientation brief card shows the sterile-phase line. (Backfilled entry — shipped without a changelog line.)',
  },
  {
    version: 'v1.91.0',
    id: 'v1-91-0',
    date: '2026-07-03',
    type: 'feature',
    headline: 'Sterile phases — per-phase strictness (escalation-only)',
    summary: 'maddu phase set --name <n> --tier strict|standard|relaxed — while the phase is active, the effective governance mode is the stricter of workspace mode and phase tier. Escalation-only: a relaxed phase tier on a strict workspace changes nothing; weakening stays an explicit governance set. The base config is never rewritten — maddu phase clear lifts the escalation and is the whole rollback. Loops, the coordinator, strict-mode approvals, and governance show all resolve through the escalated view.',
  },
  {
    version: 'v1.90.0',
    id: 'v1-90-0',
    date: '2026-07-03',
    type: 'feature',
    headline: 'Vendor-memory interop — learn sync --from-claude-memory',
    summary: 'maddu learn sync --from-claude-memory imports Claude Code’s default-on auto-memory as vendor facts with full provenance. Import-only by contract — the vendor directory is never written — and content-hash-deduped, so re-running is idempotent and an edited memory imports as a new fact. Preview by default, --adopt to write; each import rides a VENDOR_MEMORY_IMPORTED event so a memory rebuild replays faithfully.',
  },
  {
    version: 'v1.89.1',
    id: 'v1-89-1',
    date: '2026-07-03',
    type: 'patch',
    headline: 'Hook entrypoint resolved per repo layout (dogfood catch)',
    summary: 'Installing the v1.89.0 hooks on the maddu source repo itself caught hooks install writing the consumer entrypoint everywhere — nonexistent in a source checkout, so every hook errored silently (fails-open masked it). resolveHookBin now picks the entrypoint per repo layout at install time, and doctor’s stanza-currency check uses the same resolution. Consumers were never affected.',
  },
  {
    version: 'v1.89.0',
    id: 'v1-89-0',
    date: '2026-07-03',
    type: 'feature',
    headline: 'Pre-compaction governance checkpoint',
    summary: 'maddu hooks install now also wires Claude Code’s PreCompact hook — manual /compact and auto-compaction both append a COMPACTION_CHECKPOINT to the spine: the trigger, the last recorded slice-stop (the durable anchor — anything after it that wasn’t recorded did not survive), handoff currency, open approvals, active claims. Fails open — always exit 0, because a governance instrument must never break the session it observes. maddu orient auto-announces the latest checkpoint; doctor validates hook-stanza currency.',
  },
  {
    version: 'v1.88.0',
    id: 'v1-88-0',
    date: '2026-07-03',
    type: 'hardening',
    headline: 'Completion-claim gate at every slice-stop',
    summary: 'A completion-claim gate (warn) flags a live pattern of hedged completion claims ("should work", "seems to pass") on slices with no observed proof — no real GATE_RAN(ok) during the slice, no verified deliverable; self-reported flags deliberately don’t count. Deterministic: a model checking a model is a second opinion; a deterministic check against declared deliverables is evidence. Runs at every slice-stop, surfaces but never blocks, and holds warn tier for at least a quarter of own-repo data before any fail promotion.',
  },
  {
    version: 'v1.87.0',
    id: 'v1-87-0',
    date: '2026-07-03',
    type: 'feature',
    headline: 'maddu ci — the headless gate rail',
    summary: 'maddu ci runs every deterministic gate headlessly — no LLM, no network — and exits nonzero only on gates the repo pinned as required (maddu ci pin → the committed ci config). Churn-proof by construction: framework upgrades can add gates without turning anyone’s pipeline red until they re-pin. Local enforcement first — the same exit code drives a fully-offline git pre-push hook; GitHub Actions auto-detection (::error annotations + a job summary table) is optional on top.',
  },
  {
    version: 'v1.86.0',
    id: 'v1-86-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'roadmap #14 — cost-budget gate (the runaway-session guard)',
    summary: 'An opt-in, advisory cost-budget gate. With a repo carrying .maddu/config/cost-budget.json, it sums recent tokenLedger spend over the trailing window and WARNs — never FAILs — when over, so a runaway session shows in doctor/audit without blocking a land. No provider call; it only sums numbers the ledger already holds.',
  },
  {
    version: 'v1.85.0',
    id: 'v1-85-0',
    date: '2026-06-30',
    type: 'feature',
    headline: 'roadmap #8 — lesson federation (corrections compound across the fleet)',
    summary: 'maddu learn sync reads sibling repos’ agent-file corrections off the workspace registry and surfaces the portable ones — a lesson recurring in ≥2 repos or tagged @portable. --adopt writes them into this repo’s CLAUDE.md learn block, approval-only, with OS-absolute paths redacted so a sibling’s machine layout never leaks.',
  },
  {
    version: 'v1.84.0',
    id: 'v1-84-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'roadmap #13 — compat spine (read an old install safely)',
    summary: 'Makes reading an old install’s state total so maddu fleet upgrade can deliver into installs back to v1.15 without crashing on a missing key. project() stamps a schemaVersion; normalizeProjection(raw) turns any projection — current, legacy, partial, or garbage — into a total current-shape object; the can-read-old-state gate keeps the reader total.',
  },
  {
    version: 'v1.83.0',
    id: 'v1-83-0',
    date: '2026-06-30',
    type: 'feature',
    headline: 'roadmap #10-mutation — maddu fleet upgrade --apply (staged delivery)',
    summary: 'maddu fleet upgrade --apply delivers canonical framework bytes to eligible behind repos by reusing the single-repo maddu upgrade engine. Must be scoped (--only <repo> or --all); each target re-checks quiescence, snapshots the managed bytes (never the spine), upgrades, then runs doctor — the first red halts the whole run and prints a rollback path.',
  },
  {
    version: 'v1.82.0',
    id: 'v1-82-0',
    date: '2026-06-30',
    type: 'feature',
    headline: 'roadmap #10 — maddu fleet upgrade --plan (the F1 delivery leg)',
    summary: 'maddu fleet upgrade --plan — a read-only, offline preview of what a fleet delivery would do per behind repo: a quiescence interlock (active claim, dirty tree, or recent spine activity marks a repo blocked, else eligible) and the managed-byte delta vs the recorded manifest hashes. Live, it mapped 12 behind repos → 4 eligible / 8 blocked.',
  },
  {
    version: 'v1.81.0',
    id: 'v1-81-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'roadmap #5 — retire the dead skill funnel (F2)',
    summary: 'Retires the dead skill-candidate funnel (F2): the auto-detector fired but every downstream terminal was 0 across 13 installs, while maddu learn already captures the valuable corrections. The slice-stop auto-trigger is removed, SKILL_CANDIDATE_DETECTED is dispositioned dormant, and a funnel-integrity gate FAILs if it’s ever re-wired.',
  },
  {
    version: 'v1.80.0',
    id: 'v1-80-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'roadmap #12 — reposition the charter (F4)',
    summary: 'Repositions the charter so orchestration-≈0 stops reading as a dead domain (F4). Every verb in _tiers.mjs now carries a layer — core (the always-on session/lane/slice/gate substrate, 62 verbs) vs orchestration (the opt-in multi-agent layer, 4 verbs). maddu audit positioning reports orchestration as an opt-in fire-rate, never as dead.',
  },
  {
    version: 'v1.79.0',
    id: 'v1-79-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'roadmap #9 — sharpen the discipline loop',
    summary: 'Sharpens the core loop: maddu orient opens with a one-glance status line — gates green/red, goal progress, next — and a GATES section renders each hard failure legibly (gate id, the spine event id, the exact maddu doctor --gate <id> repro), never a stack trace. GATE_RAN events now carry the resolved status so a soft warn is never mislabeled a hard fail.',
  },
  {
    version: 'v1.78.0',
    id: 'v1-78-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'roadmap #7 — the governance surface is itself budgeted',
    summary: 'Budgets the governance surface itself. A self-applying cap inside maddu audit gives each governance category — gates, CLI verbs, audit checks — a ceiling read from ground truth; over a cap FAILs unless you retire something or add a waiver row (which raises the ceiling by one and shows as recorded debt).',
  },
  {
    version: 'v1.77.0',
    id: 'v1-77-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'audit sprint 3 — the outcome ledger',
    summary: 'The outcome ledger measures whether the guardrails change an outcome. outcome.mjs counts the faults the gates caught — every FAILED gate run is a recorded PREVENTED_FAULT, split hard vs soft by severity and tallied per gate — and the maddu fleet view now shows per-repo ⚿ N caught and an active-fleet headline.',
  },
  {
    version: 'v1.76.0',
    id: 'v1-76-0',
    date: '2026-06-30',
    type: 'feature',
    headline: 'audit sprint 2 — Fleet Spine + the self-verifying audit circuit',
    summary: 'Fleet Spine — maddu fleet, a read-only single-machine aggregator (F1). It walks the workspace registry and digests each repo from its on-disk projection + version.json without running it, tiers them ACTIVE/DORMANT/ABANDONED, and computes each repo’s version delta vs the fleet’s latest. The live fleet immediately showed 12/13 active repos behind.',
  },
  {
    version: 'v1.75.0',
    id: 'v1-75-0',
    date: '2026-06-30',
    type: 'hardening',
    headline: 'audit sprint 1 — structural guardrails from the 13-repo cross-project audit',
    summary: 'First audit sprint from the 13-repo cross-project audit: three guardrails that each make a fault class structurally impossible. An offline staleness FLOOR nudges maddu upgrade from the install’s own version.json date; a release-parity gate requires a FIXED-IN.json row at the release boundary; a disposition registry drops maddu insights dead count 34 → 0.',
  },
  {
    version: 'v1.74.2',
    id: 'v1-74-2',
    date: '2026-06-29',
    type: 'hardening',
    headline: '.maddu/ runtime state stays out of git — the working tree stops being perpetually dirty',
    summary: 'maddu init now ships a Policy-A .gitignore that ignores .maddu/* and re-includes only durable authored artifacts (config/skills/plans/wiki, the lane catalog), so runtime spine + projections stop churning the tree. A maddu-state-untracked doctor gate (warn) detects already-tracked rebuildable state and prints the non-destructive git rm --cached remediation.',
  },
  {
    version: 'v1.74.1',
    id: 'v1-74-1',
    date: '2026-06-29',
    type: 'patch',
    headline: 'Integrity tolerates CRLF — Windows installs stop self-reporting as tampered',
    summary: 'On a Windows tree with core.autocrlf=true, all ~360 managed files hashed differently and doctor reported a clean install as tampered while upgrade skipped every file. sha256OfFile now collapses CRLF→LF for text before hashing (binary hashed raw), and maddu init writes a Máddu-scoped .gitattributes pinning LF so fresh installs never churn.',
  },
  {
    version: 'v1.74.0',
    id: 'v1-74-0',
    date: '2026-06-29',
    type: 'feature',
    headline: 'Session discipline by default — fresh repos never build unrecorded',
    summary: 'A shared resolveSessionId (precedence --session → $MADDU_SESSION_ID → the liveness-verified active-session cache) lets one maddu register flow into lane claim/slice-stop across fresh shells with no flag. New maddu hooks wires Claude Code SessionStart/SessionEnd hooks into .claude/settings.json to auto-register and close a session.',
  },
  {
    version: 'v1.73.1',
    id: 'v1-73-1',
    date: '2026-06-28',
    type: 'patch',
    headline: 'maddu doctor stops crying wolf on consumer installs',
    summary: 'The generated-artifacts-current gate could hard-FAIL on a healthy install, pointing at scripts/generate.mjs that doesn’t ship to consumers. It now skips on the positive presence of generate.mjs (the source-checkout marker) instead of inferring consumer-vs-source from target absence, so the message and the demand can no longer disagree.',
  },
  {
    version: 'v1.73.0',
    id: 'v1-73-0',
    date: '2026-06-25',
    type: 'patch',
    headline: 'maddu debt accuracy — the ledger stops miscounting itself',
    summary: 'The deliberate-shortcut scanner miscounted itself, even stamping the best-documented marker [no-trigger] because its upgrade: trigger lived on a continuation line. scanDebt now gathers adjacent comment-continuation lines, only counts a token that begins a comment body, and skips doc files — real debt went 21/14/7 → an accurate 2/2/0.',
  },
  {
    version: 'v1.72.0',
    id: 'v1-72-0',
    date: '2026-06-25',
    type: 'feature',
    headline: 'maddu agents — "install maddu" on demand, machine-wide',
    summary: 'maddu agents <detect|register|unregister> makes "install maddu" an instruction every agent understands in every repo. It merges a self-contained install stanza into each agent’s global instruction file (Claude Code, Codex, Gemini, generic) via an idempotent marker block that preserves operator content; paths resolve from os.homedir(), never hardcoded.',
  },
  {
    version: 'v1.71.0',
    id: 'v1-71-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — command-bar module (composer + palette)',
    summary: 'Extracts the slash-command bar (composer + Ctrl-K palette) into cockpit-command-bar.js via a host-injection seam. cockpit.js drops 2423 → 1715 (−81% from the 9202-line monolith) and is now the minimal composition root — the decomposition is complete.',
  },
  {
    version: 'v1.70.0',
    id: 'v1-70-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — Inspector module (optional shell polish)',
    summary: 'Extracts the Inspector entity-detail drawer into cockpit-inspector.js (open state, ensureInspector, the five tab renderers). cockpit.js 2686 → 2423; the cockpit is now 16 modules. First of the optional post-completion polish slices.',
  },
  {
    version: 'v1.69.0',
    id: 'v1-69-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — BOSS (route-view extraction COMPLETE)',
    summary: 'Moves the last route view, BOSS (the proposal · enforcer · decision terminal), into cockpit-views-live.js. Every one of the cockpit’s 42 routes now renders from a sibling module; cockpit.js 3115 → 2686 (−71% from the monolith), a pure composition root.',
  },
  {
    version: 'v1.68.0',
    id: 'v1-68-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Conductor)',
    summary: 'Moves Conductor (Next Command, KPI strip, Now/Next/Waiting/Done board, Operation Score Matrix) into cockpit-views-live.js, and the shared reason-code palettes down to the cockpit-event-rows leaf. cockpit.js 3346 → 3115.',
  },
  {
    version: 'v1.67.0',
    id: 'v1-67-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Workbench)',
    summary: 'Moves Workbench (the 3-pane operator cockpit) into cockpit-views-live.js behind two new narrow seams ctx.refreshStatus() and ctx.onRouteLeave(). cockpit.js 3613 → 3346; a Codex consult confirmed the final trio is composer-free.',
  },
  {
    version: 'v1.66.0',
    id: 'v1-66-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Chats — last non-composer view)',
    summary: 'Moves Chats (the sessions roster), the last route view that doesn’t drive the composer, into cockpit-views-live.js with no new ctx seams. cockpit.js 3645 → 3613; only the composer-deep trio remains inline.',
  },
  {
    version: 'v1.65.0',
    id: 'v1-65-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Queue Board + Claim Map)',
    summary: 'Moves the two scheduler/lane views, Queue Board and Claim Map, into cockpit-views-live.js with zero new ctx seams; their private builders take ctx so card/row clicks reach ctx.openInspector. cockpit.js 3857 → 3645.',
  },
  {
    version: 'v1.64.0',
    id: 'v1-64-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Dashboard)',
    summary: 'Moves the headline Dashboard overview into cockpit-views-live.js behind a new cached-snapshot seam ctx.bridgeStatus() + ctx.bridgeOk(). cockpit.js 4010 → 3857 — under 4k.',
  },
  {
    version: 'v1.63.0',
    id: 'v1-63-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Orientation + Gates + Reviews)',
    summary: 'Three read-only ledger views — Orientation, Gates, and Reviews — move into cockpit-views-live.js in one slice with zero new seams. cockpit.js 4236 → 4010.',
  },
  {
    version: 'v1.62.0',
    id: 'v1-62-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Approvals)',
    summary: 'Moves the Approvals view into cockpit-views-live.js behind a new shared-read seam ctx.fetchApprovals (also called by the still-inline Workbench, so it joins ctx without duplication). cockpit.js 4390 → 4236.',
  },
  {
    version: 'v1.61.0',
    id: 'v1-61-0',
    date: '2026-06-20',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (Events stream view)',
    summary: 'Moves the live Events stream view into cockpit-views-live.js behind a new stream-pause seam ctx.isStreamPaused() + ctx.toggleStreamPause(); its handler appends each matching row live rather than refetching. cockpit.js 4482 → 4390.',
  },
  {
    version: 'v1.60.0',
    id: 'v1-60-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster (operations + swarm)',
    summary: 'Moves Operations (receipt log, hindsight memory, checkpoints) and Swarm into cockpit-views-live.js behind one new shared-read seam ctx.fetchMemory. cockpit.js 4742 → 4482.',
  },
  {
    version: 'v1.59.0',
    id: 'v1-59-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — live cluster begins (mailbox + tasks + skills)',
    summary: 'Opens cockpit-views-live.js with the first live-cluster trio: Mailbox, Tasks, and Skills, whose only shell couplings are proven ctx seams (ctx.onSpineEvent, ctx.currentSession). cockpit.js 5166 → 4742; now 14 modules.',
  },
  {
    version: 'v1.58.0',
    id: 'v1-58-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — connect cluster complete (schedule + mcp + runtimes)',
    summary: 'Extracts the last three connect views (Schedule, MCP, Runtimes) into cockpit-views-connect.js behind one new accessor ctx.scopeIsGlobal(route), finishing the connect cluster. cockpit.js 5627 → 5166.',
  },
  {
    version: 'v1.57.0',
    id: 'v1-57-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — Imports view (+ the narrow currentSession accessor)',
    summary: 'Extracts the Imports view into cockpit-views-connect.js and adds the narrow read-only ctx.currentSession() accessor (the composer’s sticky session pointer) so POST actions can stamp by:. cockpit.js 5759 → 5627.',
  },
  {
    version: 'v1.56.0',
    id: 'v1-56-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — the stream-subscription seam (+ Auth view)',
    summary: 'Introduces the event-stream subscription seam ctx.onSpineEvent(handler) (subscribe + auto-teardown on route change, with a torn-flag guard) and proves it by extracting the Auth view — which unblocks every remaining stream-coupled view. cockpit.js 5887 → 5759.',
  },
  {
    version: 'v1.55.0',
    id: 'v1-55-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — connect cluster begins (Settings + Trust)',
    summary: 'Opens the connect cluster with its two stream-free views, Settings and Trust, in a new cockpit-views-connect.js; both extracted programmatically to avoid transcription error on a ~530-line move. cockpit.js 6417 → 5887 — the largest single slice.',
  },
  {
    version: 'v1.54.0',
    id: 'v1-54-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — inspect-heavy: Plans view (cluster complete)',
    summary: 'The sixth and final inspect-heavy view, Plans (the Now/Next/Blocked/Done kanban board), joins cockpit-views-inspect.js, completing the inspect-heavy cluster; the ctx seam grows to add openEntityDrawer. cockpit.js 6569 → 6417.',
  },
  {
    version: 'v1.53.0',
    id: 'v1-53-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — inspect-heavy: Agents view (+ a narrow router-rerender seam)',
    summary: 'The Agents coworker/session grid joins cockpit-views-inspect.js, the first scope-aware view; the seam grows scopePill, scopedUrl, and a narrow ctx.rerender alias rather than handing the view the router. cockpit.js 6651 → 6569.',
  },
  {
    version: 'v1.52.0',
    id: 'v1-52-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — inspect-heavy: Roadmap view',
    summary: 'The Roadmap view (slice-stop KPIs, a 28-day closure-cadence bar, a lane-mix table, the slice index) joins cockpit-views-inspect.js with no ctx growth — its charts build inline. cockpit.js 6810 → 6651.',
  },
  {
    version: 'v1.51.0',
    id: 'v1-51-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — inspect-heavy: Workflows view',
    summary: 'The Workflows view (the architecture blueprint SVG node/edge graph, each node opening in the Inspector) joins cockpit-views-inspect.js with no ctx growth; its only shell dep, openInspector, is already on the seam. cockpit.js 6930 → 6810.',
  },
  {
    version: 'v1.50.0',
    id: 'v1-50-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — inspect-heavy: Teams view',
    summary: 'The Teams lane-ownership map joins cockpit-views-inspect.js; the seam grows the two bridge fetch helpers (fetchLanes/fetchProjection) and the palette focus pair so a ?focus=<lane> deep link flashes the card. cockpit.js 7013 → 6930.',
  },
  {
    version: 'v1.49.0',
    id: 'v1-49-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — first inspect-heavy view (Learning)',
    summary: 'Opens the inspect-heavy cluster with the Learning hindsight-memory browser in a new cockpit-views-inspect.js; the seam grows openInspector, and its fixture fires a row click to assert the interaction headlessly. cockpit.js 7148 → 7013.',
  },
  {
    version: 'v1.48.0',
    id: 'v1-48-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — Docs view extracted',
    summary: 'Pulls the Docs route renderer (the in-cockpit manual reader — markdown pages, auto-TOC, in-article link interception, a self-removing hashchange listener) into cockpit-views-docs.js; a pure leaf move, no ctx. cockpit.js 7398 → 7148.',
  },
  {
    version: 'v1.47.0',
    id: 'v1-47-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition — reference view cluster extracted',
    summary: 'Extracts the five reference route renderers (Goal, Tools, Loops, Search, Wiki) into cockpit-views-reference.js; only renderGoal needs a shell helper, injected as ctx.panelFocus. cockpit.js 7779 → 7398.',
  },
  {
    version: 'v1.46.0',
    id: 'v1-46-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Real-browser smoke gate (Playwright) + workbench first-paint fix',
    summary: 'Adds a real-browser smoke gate, cockpit-playwright.mjs — loads the shipped cockpit in headless Chromium, drives hashchange across all 42 routes, and graceful-skips when Playwright is absent. On day one it caught a TypeError aborting Workbench first-paint (updateTabs reading a count span pre-attach), now null-guarded.',
  },
  {
    version: 'v1.45.0',
    id: 'v1-45-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — ctx seam + first view module',
    summary: 'Introduces the dependency-injection ctx seam threaded through the router (route.render(ctx)) and extracts the first view module, cockpit-views-backbone.js (the six v0.18 single-panel routes). Gate B stayed byte-identical across all 43 goldens.',
  },
  {
    version: 'v1.44.0',
    id: 'v1-44-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — route-metadata split',
    summary: 'Splits the route table so view modules can import route metadata without the render graph: a new pure-data cockpit-route-meta.js holds ROUTE_META for all 42 routes, and cockpit.js rebuilds ROUTES by merging renderers onto it. Gate B byte-identical across 43 goldens.',
  },
  {
    version: 'v1.43.0',
    id: 'v1-43-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — small util leaves',
    summary: 'A batch of small standalone helpers (copyToClipboardWithToast, workspaceBadge, laneFromFact, errorState) join the cockpit-util.js leaf. Gate B stayed byte-identical across all 43 goldens.',
  },
  {
    version: 'v1.42.0',
    id: 'v1-42-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — markdown leaf',
    summary: 'The Docs route’s markdown renderer moves to a new cockpit-markdown.js leaf — a self-contained CommonMark-ish string→HTML transform with no DOM and no cockpit state. Gate B byte-identical across 43 goldens.',
  },
  {
    version: 'v1.41.0',
    id: 'v1-41-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — event-row leaf',
    summary: 'The spine-event rendering helpers (classifyEvent, eventRow, prepend, makeDecisionButton) move to a new cockpit-event-rows.js leaf. Gate B stayed byte-identical across all 43 goldens.',
  },
  {
    version: 'v1.40.0',
    id: 'v1-40-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — backbone card leaf',
    summary: 'The pure data→DOM card renderers of the v0.18 backbone routes move together to a new cockpit-backbone-cards.js leaf (−260 lines). Gate B byte-identical across 43 goldens.',
  },
  {
    version: 'v1.39.0',
    id: 'v1-39-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — loading-skeleton leaf',
    summary: 'The skeleton-placeholder helpers loading (83 call sites) and loadingFor (the shape-aware variants) move verbatim to the cockpit-util.js leaf. Gate B stayed byte-identical across all 43 goldens — no golden update.',
  },
  {
    version: 'v1.38.0',
    id: 'v1-38-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit decomposition (Phase 1) — formatter leaf',
    summary: 'The first slice riding the new headless harness: the formatters formatAge, ageTone, formatTs move to the cockpit-util.js leaf — the first cockpit slice in project history verified without an operator browser refresh. Gate B byte-identical across 43 goldens.',
  },
  {
    version: 'v1.37.0',
    id: 'v1-37-0',
    date: '2026-06-19',
    type: 'polish',
    headline: 'Cockpit verification harness (Phase 0)',
    summary: 'Makes the cockpit SPA verifiable headlessly: a happy-dom env, Gate A (cockpit-boot renders all 42 routes without a throw) and Gate B (cockpit-snapshot diffs every route’s DOM against 43 committed goldens). The only shipped change is a two-line prod-identical seam, so future slices self-verify like the server slices did.',
  },
  {
    version: 'v1.36.1',
    id: 'v1-36-1',
    date: '2026-06-18',
    type: 'polish',
    headline: 'Docs sweep — architecture refactor',
    summary: 'Docs-only patch closing the two doc gaps the v1.19.0→v1.36.0 architecture refactor left behind: 03-cli-reference.md now lists maddu architecture mass, and 00-index.md’s version banner refreshes from a stale v1.12.0 to v1.36.0 / 62 verbs / audit 14/14. No code or behavior change.',
  },
  {
    version: 'v1.36.0',
    id: 'v1-36-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (18) — cockpit split, slice 3 (comms panels + shared toast)',
    summary: 'The three comms-plugin settings panels move to a new cockpit-comms.js, and showToast becomes a proper cockpit-util.js leaf for reuse (~130 call sites). cockpit.js 8942 → 8555; still the sole monolith.',
  },
  {
    version: 'v1.35.0',
    id: 'v1-35-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (17) — cockpit split, slice 2 (widget kit)',
    summary: 'The widget kit (statusGrid, bar, segBar, donut, sparkline, meter, binByTime) moves to a new cockpit-widgets.js leaf — pure inline SVG/DOM, no chart lib, no back-reference to cockpit state. cockpit.js 9202 → 8942.',
  },
  {
    version: 'v1.34.0',
    id: 'v1-34-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (16) — server split, slice 10 (collaboration routes)',
    summary: 'The BOSS / proposal collaboration subsystem (routeProposals, routeBoss) moves to bridge-routes-collab.mjs. server.js 1193 → 1063 — ~61% off the original bridge monolith.',
  },
  {
    version: 'v1.33.0',
    id: 'v1-33-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (15) — server split, slice 9 (work-execution routes)',
    summary: 'The five work-execution route groups (routeWorkers, routeSkills, routeTasks, routeMailbox, routeMemory) move to bridge-routes-work.mjs. server.js 1434 → 1193 — ~56% off the bridge monolith.',
  },
  {
    version: 'v1.32.0',
    id: 'v1-32-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (14) — server split, slice 8 (capability routes) · server.js is no longer a monolith',
    summary: 'Four capability route groups (imports/auth/checkpoints/schedules) move to bridge-routes-capabilities.mjs. server.js 1576 → 1434 — dropping below the 1500-line threshold, so it is no longer a monolith and the mass ratchet now tracks only cockpit.js.',
  },
  {
    version: 'v1.31.0',
    id: 'v1-31-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (13) — server split, slice 7 (approval-gateway routes)',
    summary: 'The approval gateway (routeApprovals, with its per-repo → global auto-decide cascade) moves to bridge-routes-approvals.mjs. server.js 1657 → 1576.',
  },
  {
    version: 'v1.30.0',
    id: 'v1-30-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (12) — server split, slice 6 (lane-ownership route groups)',
    summary: 'The lane-ownership subsystem (routeSessions, routeLanes — the substrate behind rule #8) moves to bridge-routes-lanes.mjs. server.js 1837 → 1657.',
  },
  {
    version: 'v1.29.0',
    id: 'v1-29-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (11) — server split, slice 5 (registry route groups)',
    summary: 'The first route-group extraction out of the handleBridge if-chain. The two registry CRUD groups (routeMcp, routeRuntimes) move to bridge-routes-registries.mjs behind a new reusable dispatch contract (route<Group> returns true when it owns the path, false to fall through). server.js 1941 → 1837.',
  },
  {
    version: 'v1.28.0',
    id: 'v1-28-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (10) — server split, slice 4 (bootstrap helpers)',
    summary: 'The bridge’s pre-listen bootstrap helpers (resolveRepoRoot, detectFrameworkLayout, readVersion, pickPort, probePortIsMaddu, findPidOnPort) move to bridge-bootstrap.mjs. server.js 2035 → 1941.',
  },
  {
    version: 'v1.27.0',
    id: 'v1-27-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (9) — server split, slice 3 (workspace fan-out)',
    summary: 'The /bridge/_all/* multi-workspace fan-out helpers (fanoutProjection/fanoutConductor/fanoutApprovals/fanoutQueue/fanoutEventsRecent) move to bridge-fanout.mjs. server.js 2237 → 2035 — ~25% off the bridge monolith.',
  },
  {
    version: 'v1.26.0',
    id: 'v1-26-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (8) — server split, slice 2 (projection builders)',
    summary: 'The four cockpit projection builders (buildConductor, buildQueueBoard, buildClaimMap, buildBacklinks) move to bridge-builders.mjs. A live-bridge boot caught two interspersed-shared-helper bugs instantly — the dividend of splitting the verifiable monolith first. server.js 2630 → 2237.',
  },
  {
    version: 'v1.25.0',
    id: 'v1-25-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (7) — server split, slice 1 (HTTP transport)',
    summary: 'Begins decomposing the 2705-line server.js bridge monolith along HTTP boundaries, pure helpers first. The seven transport helpers (send, sendJson, readBody, serveStatic, the loopback-hostname guard, …) move to http-util.mjs. server.js 2705 → 2630.',
  },
  {
    version: 'v1.24.0',
    id: 'v1-24-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (6) — cockpit split, slice 1 (prove the pattern)',
    summary: 'Begins decomposing the 9260-line cockpit.js SPA monolith into browser-native ES modules — no bundler, no build step. The six pure leaf utilities (el, panel, placeholder, formatUptime, compactPath, truncatePathFromLeft) move to a sibling cockpit-util.js. cockpit.js 9260 → 9202.',
  },
  {
    version: 'v1.23.0',
    id: 'v1-23-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (5) — structural mass: the monolith ratchet',
    summary: 'maddu architecture gains a second dimension: file mass. maddu architecture mass reports per-file line counts and exact-duplicate files, and a shrink-only architecture-mass gate grandfathers cockpit.js (9260) and server.js (2705) into a baseline so they can only get smaller. maddu audit 13 → 14.',
  },
  {
    version: 'v1.22.0',
    id: 'v1-22-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (4) — retire the policing gate (the proof)',
    summary: 'Retires the docs-in-sync policing gate now that the doc tree is generated (generated-artifacts-current asserts byte-exact, stronger), proving the single-sourcing worked. rule-invariant is repurposed — not deleted — as a substance canary over the generated briefs. maddu audit 14 → 13.',
  },
  {
    version: 'v1.21.0',
    id: 'v1-21-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (3) — the doc tree is generated, not hand-mirrored',
    summary: 'The bundled docs payload is now generated from the authored docs/ source, not hand-mirrored. A new EOL-preserving mirror generator expands docs/*.md → template/maddu/docs/*.md (48 files) with zero churn; generated-artifacts-current now covers 52 artifacts.',
  },
  {
    version: 'v1.20.1',
    id: 'v1-20-1',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (2b) — the compact rule stanza joins the registry',
    summary: 'The third brief surface, the compact .section.md rule stanza, is now generated from the same rules.json registry, so all four agent briefs derive their hard-rules text from one source. With that, the rule-invariant drift gate is fully retireable.',
  },
  {
    version: 'v1.20.0',
    id: 'v1-20-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (2) — rule-registry single-sourcing',
    summary: 'The 8+1 hard rules, previously spelled out by hand in two verbose briefs, are now authored once in rules.json and generated into both via an EOL-aware marker-injection engine. A byte-exact migration — the only change is the two marker lines — so rule-invariant still passes.',
  },
  {
    version: 'v1.19.0',
    id: 'v1-19-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Architecture refactor (1/3) — guardrail · loader-dedup · gen-discipline',
    summary: 'Three slices that lock in the clean import layering: a 10-module target contract enforced by maddu audit (failOn:new), loader-dedup converging six hand-rolled lib loaders onto two single sources (−91 lines), and a gen-discipline boundary — a deterministic generation engine + a generated-artifacts-current drift gate.',
  },
  {
    version: 'v1.18.2',
    id: 'v1-18-2',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Capability docs — debt deep-dive, slice-stop guards, verb→doc map',
    summary: 'Doc-completeness pass matching the post-v1.16 surface: a maddu debt deep-dive (docs/41-debt.md), the v1.17.0 slice-stop change-risk + deliverable guards documented in the canonical ritual doc, and a machine-readable verb→doc map (capability-docs.json) for all 62 verbs. A new capability-docs audit check keeps the map honest — maddu audit 12 → 13.',
  },
  {
    version: 'v1.18.1',
    id: 'v1-18-1',
    date: '2026-06-18',
    type: 'patch',
    headline: 'Charter coherence — architecture is a capability verb',
    summary: 'A coherence patch surfaced while mirroring the charter into the marketing site: maddu architecture shipped in v1.18.0 but was never in the charter’s capability-verb table, and charter-drift passed it anyway by matching the prose word "architecture". The verb is added under Quality & review (62 verbs), and charter-drift now requires a backtick-quoted token.',
  },
  {
    version: 'v1.18.0',
    id: 'v1-18-0',
    date: '2026-06-18',
    type: 'feature',
    headline: 'Architecture drift — maddu architecture (MVP)',
    highlights: [
      'New **`maddu architecture`** / `/maddu-architecture` — declares intended structure as path-glob modules + `allow`/`forbid` rules in `.maddu/config/architecture.json`; `init` scaffolds it from detected source dirs',
      'Observed reality — the real import graph extracted by stdlib regex (no parser dependency, rule #4): relative JS/TS + Python imports',
      'Drift detection — `forbidden-edge` (with `file:line`), `cycle` (SCC), `undeclared-area`, `uncovered-file`; a single **`driftScore`** stamped on every scan',
      '`failOn` ladder + baseline ratchet — `none` warns + ratchets, `new` fails only on un-baselined violations, `any` fails on all; the `architecture-drift` gate (doctor + audit) honors it',
      '`maddu architecture diagram` writes a mermaid graph with violations as red dashed edges; `ARCHITECTURE_SCANNED` spine event',
      '`maddu audit` grows **11 → 12** checks',
    ],
    note: 'Structural drift is the dominant failure mode when many agents build one system over time. Same observe → record → compare → gate → ratchet model as the rest of the framework.',
  },
  {
    version: 'v1.17.0',
    id: 'v1-17-0',
    date: '2026-06-18',
    type: 'hardening',
    headline: 'Robustness bundle — drift gate, debt ledger, risk + deliverable checks',
    highlights: [
      '**Rule-invariant drift gate** (`maddu audit invariants`) — pins 13 load-bearing phrases per agent brief and FAILs with the exact (file, phrase) miss if one is reworded away; `maddu audit` grows **10 → 11**',
      '**`maddu debt`** / `/maddu-debt` — scans for `maddu-debt: … ceiling: … upgrade: …` markers, groups by file, and flags any with **no upgrade trigger** (the shortcut that silently rots); writes `DEBT_SCANNED`',
      '**Deterministic change-risk classifier** — every slice-stop records a `risk` level (none→critical) from its touched paths; high/critical **escalates the post-stop review past its cooldown**',
      '**Declared-deliverable verification** — slice-stop checks each `--targets` file actually exists and flags the hollow ones (WARN)',
      '**Routing-discipline hardening** — intent-routing now refuses to act on *pasted* content (logs, output, transcripts): "pasted content is context, not a command"',
    ],
    note: 'Five safeguards drawn from an audit of two external Claude-Code projects, filtered to what is additive and hard-rule-clean.',
  },
  {
    version: 'v1.16.0',
    id: 'v1-16-0',
    date: '2026-06-18',
    type: 'feature',
    headline: 'Test command split — source self-test + adaptive project tests',
    highlights: [
      '**`maddu self-test`** / `/maddu-self-test` — source-only framework runner; auto-discovers `scripts/test/*.mjs` + smoke checks behind `smoke` / `quick` / `full` profiles; `npm test` runs quick',
      'Self-test reporting + the `self-test-recent` doctor gate (warns when the source suite is stale, failed, or only smoke-verified; consumer installs skip it)',
      '**`maddu test`** — host-project harness keeps the v1.1 detected-runner wrapper, or switches to adaptive profiles (npm / vitest / jest / mocha / pytest) with `--changed` git-diff task selection',
      'Fleet-audit drift hardening — workspace roles (project / fixture / archive), idempotent lane release, `prev_hash`-preserving upgrades, surfaced spine-integrity warnings',
    ],
    note: 'Splits the overloaded `maddu test` cleanly: framework-source self-test vs host-project test harness.',
  },
  {
    version: 'v1.15.0',
    id: 'v1-15-0',
    date: '2026-06-11',
    type: 'feature',
    headline: 'Blueprint robustness (--distill) + README rework',
    highlights: [
      'Blueprint extractor **hardened + pinned** — malformed / truncated / null transcript lines are skipped not thrown; Windows backslash paths normalize; the output contract holds on all-empty input',
      '`--since` now **fails loud** (exit 2) instead of silently widening the export',
      '**`maddu blueprint --distill`** — spawns a provider CLI to rewrite the deterministic skeleton into flowing prose (`*-distilled.md`); the deterministic export stays **canonical**; emits `BLUEPRINT_DISTILLED`',
      'Best-effort with graceful fallback — no sign-in / worker failure falls back to the deterministic blueprint, exit 0 (rule #5: the provider call lives only in the subprocess)',
      'Shared `_worker-spawn.mjs` extracted — `learn` + `blueprint --distill` single-source the audited spawn + auth-gate logic',
      '**README** reorganized from version-archaeology into a product-first description of what Máddu is today',
    ],
  },
  {
    version: 'v1.14.0',
    id: 'v1-14-0',
    date: '2026-06-09',
    type: 'hardening',
    headline: 'Spine tamper-evidence — forward prev_hash chain',
    highlights: [
      '**Forward `prev_hash` chain** — every event carries the SHA-256 of the preceding event\'s stored NDJSON line (genesis is `null`); a ≤64 KB tail-read keeps the cost flat',
      '**`chain-integrity` in `spine verify`** — recomputes the chain and flags the first broken link (`chain_broken`) or an unchained `chain_gap`, pinpointing where history was altered',
      '**WARN, not FAIL** — the no-mutex append path can also fork the chain, so the verifier reports and the operator adjudicates tamper vs. concurrency; **never auto-repaired** (rule #2)',
      'Forward-only — no migration; legacy events keep verifying clean and adopt the chain on their next append',
      'Pure stdlib `crypto` — no new dependency (rule #4), every line stays independently `cat`-able (rule #1)',
    ],
    note: 'Upgrades the spine from legible to tamper-evident. Charter-level decision: the spine should be trustworthy independent of git.',
  },
  {
    version: 'v1.13.0',
    id: 'v1-13-0',
    date: '2026-06-09',
    type: 'hardening',
    headline: 'Robustness hardening — tighten the guarantees',
    highlights: [
      'Framework-repo `doctor` is **self-explanatory** — the expected `maddu.json missing` FAIL in the source repo is now an INFO line, detected structurally',
      '**Append durability + torn-line detection** — `append()` passes explicit `O_APPEND`; `spine verify` distinguishes a safe `torn_trailing_line` from interior `unparseable` corruption',
      '**Bridge loopback-origin enforcement** — requests whose Host/Origin hostname is not loopback are rejected `403 forbidden_origin` (DNS-rebinding defense), with a rate-limited event',
      '**Blueprint output secret-scanned** — `redactText()` scrubs every rendered blueprint before it is written',
      '**Hard-rule ↔ gate traceability** — new doc + a `rule-gate traceability` audit sub-check; rules 3 and 7 documented as enforced-by-construction',
      'Stress harness grows to **15 scenarios**; `docs-in-sync`, git-diff slice-scope, and `learn-corrections-coherent` gates wired in',
    ],
    note: 'Every change tightens an existing guarantee — no SQLite, no daemon, no queue, no new dependency, no spine auto-repair.',
  },
  {
    version: 'v1.12.0',
    id: 'v1-12-0',
    date: '2026-06-09',
    type: 'feature',
    headline: 'Project blueprint — maddu blueprint',
    highlights: [
      '**`maddu blueprint`** / `/maddu-blueprint` — exports a portable, variable-driven handoff of how a whole project was built',
      'Captures intake schema, procedure, problems & fixes, iteration hotspots, the real product repo, and acceptance criteria',
      'Deterministic and byte-stable — two renders over identical input are identical (timestamp isolated as the only variable)',
      'Secret-scanned before write — keys pasted into a transcript can\'t ride the handoff across the rule-#6 boundary',
    ],
    note: 'Reproduce a project\'s genesis in a non-Máddu context — the procedure, not just the artifact.',
  },
  {
    version: 'v1.11.0',
    id: 'v1-11-0',
    date: '2026-06-09',
    type: 'hardening',
    headline: 'Drift-proofing — single-source config seeding + coherence guards',
    highlights: [
      'All config defaults extracted to a single `_config-seed.mjs`, eliminating seed drift',
      '**`defaults-single-sourced`** gate (FAIL) — verifies defaults are never duplicated',
      '**`brief-coherence`** gate (WARN) — verifies the brief never duplicates seeded config',
      '`maddu audit` grows **6 → 8** checks',
    ],
  },
  {
    version: 'v1.10.0',
    id: 'v1-10-0',
    date: '2026-06-09',
    type: 'feature',
    headline: 'Invocation-logic pass 2 — light up the dead domains',
    highlights: [
      '**`slice-stop:auto-handoff`** — derives and emits a resume narrative at every slice-stop',
      '**Skill-candidate detection generalizes** to real products, not just Máddu itself',
      '**Auto-review** spawns after slice-stop with cooldown guards',
    ],
    note: 'Lights up three previously-dead triggers — all under the rule-#9 gauntlet.',
  },
  {
    version: 'v1.9.2',
    id: 'v1-9-2',
    date: '2026-06-09',
    type: 'patch',
    headline: 'Name maddu learn in the worker brief',
    highlights: [
      'Adds `maddu learn digest / run / memory` to the installed worker brief so agents discover the capability directly',
    ],
  },
  {
    version: 'v1.9.1',
    id: 'v1-9-1',
    date: '2026-06-09',
    type: 'patch',
    headline: 'maddu learn — Windows judgment-worker spawn fix',
    highlights: [
      'Routes the judgment prompt via STDIN instead of argv, and uses a shell only for bare `.cmd` shims',
    ],
  },
  {
    version: 'v1.9.0',
    id: 'v1-9-0',
    date: '2026-06-09',
    type: 'feature',
    headline: 'Failure learning — maddu learn',
    highlights: [
      '**`maddu learn`** / `/maddu-learn` — mines failed→succeeded tool-call pairs from Claude Code transcripts',
      'Distils durable project corrections into the brief and `.maddu/memory.ndjson`',
      'Reversible briefings + supersession chains (`LEARN_CORRECTION_WRITTEN`, `MEMORY_FACT_SUPERSEDED`)',
    ],
    note: 'Inspired by Headroom — turn past failures into standing guidance the next agent inherits.',
  },
  {
    version: 'v1.8.0',
    id: 'v1-8-0',
    date: '2026-06-05',
    type: 'hardening',
    headline: 'Rule scope boundary — Máddu’s rules govern Máddu, not your product',
    highlights: [
      'The 8+1 hard rules are explicitly scoped to **the framework layer only**',
      'Agents building *with* Máddu may use any SDK, backend, or database for their own product — the rules never constrain the host repo',
      'A scope sweep eradicates residual unscoped rule prose across the agent briefs',
    ],
  },
  {
    version: 'v1.7.0',
    id: 'v1-7-0',
    date: '2026-06-04',
    type: 'feature',
    headline: 'Invocation logic — wire WHEN the still-dead domains fire',
    highlights: [
      'Each still-dormant capability gets a defined trigger condition wired into the flow',
      '`trust-audit` fires on deps-change; the coordinator checkpoints before runs',
      'A `dormant-by-design` registry honestly separates broken from merely-untriggered features',
    ],
  },
  {
    version: 'v1.6.0',
    id: 'v1-6-0',
    date: '2026-06-04',
    type: 'feature',
    headline: 'Orchestration handoff — goal progress + cross-session briefing',
    highlights: [
      '**`maddu orient`** — runs goal verification commands and shows success-progress (✓ / ○ / ?)',
      '**`maddu handoff set`** — curates a cross-session "▶ RESUME HERE" briefing that persists across sessions',
      'Goal success conditions become measurable and verifiable (`HANDOFF_SET`, enhanced `GOAL_DECLARED`)',
    ],
  },
  {
    version: 'v1.5.0',
    id: 'v1-5-0',
    date: '2026-06-03',
    type: 'feature',
    headline: 'Real sub-worker spawn + tracking',
    highlights: [
      'The coordinator spawns **tracked** sub-workers per phase across disjoint lanes',
      '**`maddu team spawn`** fans work concurrently',
      'OAuth sub-worker tracking enables genuine multi-agent coordination (`WORKER_SPAWNED` / `WORKER_EXITED`, `TEAM_*`)',
    ],
  },
  {
    version: 'v1.4.0',
    id: 'v1-4-0',
    date: '2026-06-03',
    type: 'feature',
    headline: 'Empirical insights, plugin loader, tighter core',
    highlights: [
      'New read-only `maddu insights` / `/maddu-insights` — harvests `.maddu` spines + transcripts and classifies every event type **load-bearing / occasional / single-project / dormant / dead**',
      'New `maddu plugin` / `/maddu-plugin` (`list | info | enable | disable`) — capabilities ship as a `plugin.json` manifest, loaded only when enabled; user plugins gated by `--trust` + sha256',
      'Comms (Telegram / Discord / Email) demoted to the first plugin — out of the bridge boot path, **off by default**, back-compat auto-enable on `upgrade`',
      'Skills funnel fires automatically at `maddu slice-stop` — emits `SKILL_CANDIDATE_DETECTED` across the rule-#9 gauntlet',
      'Coordinator enforces the review stage — reviews the newest slice per phase (`SLICE_REVIEWED` + `FOLLOWUP_OPENED`)',
      'Usage audit: 90 of 142 event types never fired in real projects; honest core-dead count drops 90 → 71 after the comms carve-out',
    ],
    note: 'The first data-driven release. After ~8 real projects, a cross-project usage audit (8 spines + 125 transcripts) drove domain demotion, not deletion. The 8+1 hard rules are unchanged.',
  },
  {
    version: 'v1.3.0',
    id: 'v1-3-0',
    date: '2026-05-24',
    type: 'feature',
    headline: 'Coherence realignment + completeness',
    highlights: [
      'Three default pipelines seeded by `maddu init`: `ship-a-feature` (default), `fix-a-bug`, `plan-and-delegate`',
      '`maddu audit` charter-drift command + four new gates (event-types-reachable, command-surface-coherent, cockpit-routes-reachable, docs-indexed)',
      '`docs/charter.md` published as the canonical north star; one canonical flow throughout the docs',
      'Forgiving CLI verbs — `goal set "x"`, `task create "x"`, `plan add-phase "x"` now accept natural positional form',
      'Slash on-ramp triage: 53 verbs tagged surface = agent | operator; 4 new agent slashes added',
      '`makeId` consolidated into spine; `runWrapper` deduped across tools; ~180 LoC removed',
    ],
    note: 'Closed the audit-driven feature-sprawl diagnosis. Every cockpit route now reachable from the default flow.',
  },
  {
    version: 'v1.2.3',
    id: 'v1-2-3',
    date: '2026-05-24',
    type: 'polish',
    headline: 'Entity drawer',
    highlights: [
      'Clickable plans + kanban cards open a right-side detail drawer',
      'Reusable `openEntityDrawer({ title, subtitle, body, onClose })` primitive for future routes',
      'New `GET /bridge/plans/<id>` endpoint',
      'CSS `.entity-drawer*` slide-from-right animation with Esc / scrim / × dismissal',
    ],
  },
  {
    version: 'v1.2.2',
    id: 'v1-2-2',
    date: '2026-05-24',
    type: 'polish',
    headline: 'Cockpit path polish',
    highlights: [
      'Compact `<drive>/.../basename` display in the rail Path row',
      'Click-to-copy with toast + keyboard activation',
      'Scope-pill (`This workspace | All workspaces`) active-class now moves on click',
    ],
  },
  {
    version: 'v1.2.1',
    id: 'v1-2-1',
    date: '2026-05-24',
    type: 'polish',
    headline: 'Multi-bridge polish',
    highlights: [
      'Port-collision refusal with actionable copy (foreign-bridge detection)',
      '`maddu bridges list` + `maddu bridges kill-all`',
      'CWD-vs-workspace-registry triage at start time',
      'Cockpit rail-foot Workspace + Path rows',
      '`workspace add --path` argv alias',
    ],
  },
  {
    version: 'v1.2.0',
    id: 'v1-2-0',
    date: '2026-05-24',
    type: 'hardening',
    headline: 'Supply-chain hardening + Hermes adapter',
    highlights: [
      '`maddu trust audit / pin / verify / list / report`',
      'MCP SHA256 provenance on every install',
      'Worker env allowlist with default-deny for secret-keyed vars (enforced by `worker-env-policy-coherent` gate)',
      'Secret scan in tool argv — AWS / OpenAI / Anthropic / GitHub / GitLab / Slack patterns; raw values never logged',
      'Skill provenance enforcement, strict-mode approval enforcement',
      'New Trust cockpit route + `docs/34-threat-model.md` (the operator security manual)',
      'Hermes runtime adapter (boundary-respecting)',
    ],
    note: 'The release that made the 8+1 posture structurally enforced, not just architecturally stated.',
  },
  {
    version: 'v1.1.2',
    id: 'v1-1-2',
    date: '2026-05-23',
    type: 'patch',
    headline: 'Narrative refresh + P3 patches',
    highlights: [
      'README badge + tour version refresh',
      'Getting-started autonomy section',
      'Python cross-stack detector (pytest / ruff / black / uv / pip)',
      'Refusal text trim',
      'Skill candidate threshold (N=2 high + N=1 soft with 24h cooldown)',
    ],
  },
  {
    version: 'v1.1.1',
    id: 'v1-1-1',
    date: '2026-05-22',
    type: 'patch',
    headline: 'Burn-in patch (12 fixes)',
    highlights: [
      'P0 Windows spawn EINVAL fix (`shell: true` for npm-family commands)',
      'ralph verify exit-code, plan `--plan` argv collision, lanes-catalog-parseable gate',
      'kanban phases aggregation, workspace activate bridge reroute warning',
      '`maddu stop` + SIGINT handler, `--help` before flag validation everywhere',
      'Install validation',
    ],
  },
  {
    version: 'v1.1.0',
    id: 'v1-1-0',
    date: '2026-05-22',
    type: 'feature',
    headline: 'Autonomy + planning + tool gateway',
    highlights: [
      'Nine phases shipped: default tools (git / test / format / lint / install), MCP template gallery, governance tiers (strict / standard / relaxed)',
      'Receipt log + Operations feed',
      'Kanban + plan persistence + auto-revision',
      'Loops (ralph + plan-loop)',
      '**Coordinator as Máddu primitive** — runtime-agnostic multi-phase driver, no longer tied to Claude Code\'s Agent tool',
      'Blast + skills starter pack + autonomous skill curation',
    ],
    note: 'First release where autonomous orchestration works on Codex / Gemini / future runtimes for free.',
  },
  {
    version: 'v1.0.5',
    id: 'v1-0-5',
    date: '2026-05-22',
    type: 'patch',
    headline: 'Docs sweep — Phase-X leakage cleared',
    highlights: [
      'Deleted `docs/maddu-v0.3-roadmap.md`',
      'Cleaned Phase markers from 4 user docs',
    ],
  },
  {
    version: 'v1.0.4',
    id: 'v1-0-4',
    date: '2026-05-22',
    type: 'patch',
    headline: 'Generic default lane catalog',
    highlights: [
      'Replaced Máddu-internal lanes (cockpit-shell, bridge-server, Phase A1 / B6 / C1) with 7 generic lanes',
      'architecture · frontend · backend · infra · tests · docs · general',
    ],
  },
  {
    version: 'v1.0.3',
    id: 'v1-0-3',
    date: '2026-05-22',
    type: 'patch',
    headline: 'Framework-only routes hidden on consumer installs',
    highlights: [
      '`frameworkOnly: true` flag in ROUTES',
      'teststatus route hidden by default',
      'Reusable affordance for any future framework-internal surface',
    ],
  },
  {
    version: 'v1.0.2',
    id: 'v1-0-2',
    date: '2026-05-22',
    type: 'patch',
    headline: 'Stage-scroll hotfix',
    highlights: [
      'CSS grid + overflow fix: `min-height: 0` on `.stage` + `.stage-body`',
      'Composer no longer dragged below viewport on long route content',
    ],
  },
  {
    version: 'v1.0.1',
    id: 'v1-0-1',
    date: '2026-05-22',
    type: 'patch',
    headline: 'Cockpit UX patch',
    highlights: [
      'Anchor composer (page never scrolls; only internal panes do)',
      'Collapse sidenav',
      'Recent group with localStorage persistence',
    ],
  },
  {
    version: 'v1.0.0',
    id: 'v1-0-0',
    date: '2026-05-22',
    type: 'milestone',
    headline: 'Stable declaration',
    highlights: [
      'Public stability commit — no code change',
      'Public-surface stability for: the 8+1 hard rules, the spine NDJSON format, the 13 slash command names, the cockpit routes, the CLI surface, the marker discipline',
    ],
    note: '"From here forward, breaking these is a major version bump."',
  },
  {
    version: 'v0.19.2',
    id: 'v0-19-2',
    date: '2026-05-21',
    type: 'polish',
    headline: 'UX polish',
    highlights: [
      'Slash output re-print pattern',
      '6 dedicated cockpit routes — Cost, Advisors, Skill Injections, Model Routing, Test Status, Pipelines',
    ],
  },
  {
    version: 'v0.19.1',
    id: 'v0-19-1',
    date: '2026-05-21',
    type: 'patch',
    headline: 'Burn-in patch (11 fixes)',
    highlights: [
      'Slash command marker regression',
      'Display-vs-summarize discipline',
      '`MADDU_SESSION_ID` env honoring',
      'Slice-stop positional summary',
      'Port-in-use gate false-positive',
      'Missing bridge REST endpoints',
      'claude-code transcript import',
      '`/maddu-suggest` (13th slash)',
    ],
  },
  {
    version: 'v0.19.0',
    id: 'v0-19-0',
    date: '2026-05-21',
    type: 'feature',
    headline: 'Completeness + hardening',
    highlights: [
      'Token ledger emission via worker wrappers',
      'Advisor subprocess spawn',
      'Skill auto-injection',
      'Model routing hints',
      'Stress harness (8 scenarios)',
      'Upgrade matrix (4 source versions)',
      '7 new doctor gates',
      '**Rule #9 — the auto-trigger gauntlet — becomes permanent**',
    ],
    note: 'The release where rule #9 graduated from "candidate" to "permanent + enforced."',
  },
  {
    version: 'v0.18.0',
    id: 'v0-18-0',
    date: '2026-05-21',
    type: 'feature',
    headline: 'OMC-inspired UX shell',
    highlights: [
      '12 slash commands as the operator on-ramp',
      'Intent routing wired in MADDU.md / CLAUDE.md / AGENTS.md',
      '`maddu suggest` engine',
      'Multi-runtime agnostic surface',
    ],
    note: 'The release that made "zero learning curve" the design north star — operators stopped needing the verbose CLI.',
  },
  {
    version: 'v0.17.1',
    id: 'v0-17-1',
    date: '2026-05-20',
    type: 'milestone',
    headline: 'Arc starting point',
    highlights: [
      '18 doctor gates PASS · baseline before the v1.0 push',
      'Layout-refusal at 4/4 (invariant preserved through the entire arc that followed)',
    ],
    note: 'The reference point. Every release after this had to keep doctor green and layout-refusal at 4/4.',
  },
];

const indexItems = RELEASES.map((r) => ({
  n: r.version,
  id: r.id,
  tone:
    r.type === 'milestone' ? 'brand' :
    r.type === 'feature' ? 'accent' :
    r.type === 'hardening' ? 'danger' :
    r.type === 'polish' ? 'accent-2' :
    'warn',
  label: r.headline,
}));

function typeMeta(t: ReleaseType) {
  switch (t) {
    case 'milestone': return { tone: 'brand', label: 'milestone' };
    case 'feature':   return { tone: 'accent', label: 'feature' };
    case 'hardening': return { tone: 'danger', label: 'hardening' };
    case 'polish':    return { tone: 'accent-2', label: 'polish' };
    case 'patch':     return { tone: 'warn', label: 'patch' };
  }
}
---

<Layout title="Changelog" pathname="/changelog">
  <section class="page-head">
    <div class="container">
      <div class="eyebrow-chain">
        <span class="eyebrow">Changelog</span>
        <span class="eyebrow-sep">·</span>
        <span class="eyebrow-dim">v0.17.1 → v1.92.2</span>
        <span class="eyebrow-sep">·</span>
        <span class="eyebrow-dim">{RELEASES.length} releases · ~6 weeks</span>
      </div>
      <h1>{RELEASES.length} releases. <span class="dim-h1">One charter held.</span></h1>
      <p class="lede">
        The arc that took Máddu from a private 18-gate baseline to v1.92.2 — spine tamper-evidence, failure learning, portable blueprints, a debt ledger, architecture-drift detection, a fully decomposed cockpit, the offline fleet view, a cross-project audit program, the headless <code>maddu ci</code> gate rail, sterile phases, and recommend-only earned autonomy graded from the verified record. Every release kept the layout-refusal invariant at 4/4, took the doctor from 18 → 69 gates, and never broke a posted invariant. Color-coded by release type — milestones, features, hardening, polish, patches.
      </p>

      <dl class="cl-stats">
        <div><dt>Releases</dt><dd>{RELEASES.length}</dd></div>
        <div><dt>Doctor gates added</dt><dd>+51</dd></div>
        <div><dt>Audit self-checks</dt><dd>16</dd></div>
        <div><dt>Invariants broken</dt><dd>0</dd></div>
      </dl>

      <ul class="cl-legend">
        <li><span class="cl-dot cl-dot--brand"></span> Milestone</li>
        <li><span class="cl-dot cl-dot--accent"></span> Feature</li>
        <li><span class="cl-dot cl-dot--danger"></span> Hardening</li>
        <li><span class="cl-dot cl-dot--accent-2"></span> Polish</li>
        <li><span class="cl-dot cl-dot--warn"></span> Patch</li>
      </ul>
    </div>
  </section>

  <section class="section">
    <div class="container-wide">
      <div class="cl-layout">
        <div class="cl-releases">
          {RELEASES.map((r) => {
            const meta = typeMeta(r.type);
            return (
              <article class:list={['cl-release', `cl-release--${meta.tone}`]} id={r.id}>
                <header class="cl-head">
                  <div class="cl-head-row">
                    <span class="cl-version">{r.version}</span>
                    <span class:list={['cl-type', `cl-type--${meta.tone}`]}>{meta.label}</span>
                    <span class="cl-date">{r.date}</span>
                  </div>
                  <h3 class="cl-headline">{r.headline}</h3>
                </header>
                {r.highlights && r.highlights.length > 0 ? (
                  <ul class="cl-highlights">
                    {r.highlights.map((h) => <li set:html={h.replace(/`([^`]+)`/g, '<code>$1</code>').replace(/\*\*([^*]+)\*\*/g, '<strong>$1</strong>')}></li>)}
                  </ul>
                ) : r.summary ? (
                  <p class="cl-summary" set:html={r.summary.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/`([^`]+)`/g, '<code>$1</code>').replace(/\*\*([^*]+)\*\*/g, '<strong>$1</strong>')}></p>
                ) : null}
                {r.note && (
                  <p class="cl-note">{r.note}</p>
                )}
              </article>
            );
          })}
        </div>
        <TocIndex
          eyebrow={`Releases · ${RELEASES.length}`}
          items={indexItems}
        />
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <Callout tone="ok" title="What never moved" tag="charter discipline">
        <p>
          Every release across this arc kept the layout-refusal gate at 4/4. The 8+1 hard rules graduated from "architectural" to "doctor-enforced" without a single rule ever loosening. The spine format, the slash names, the cockpit routes, the CLI surface, the marker discipline — all stable since v1.0.0.
        </p>
        <p>
          The lesson: invariants you can't change become invariants you don't have to argue about. Velocity comes from the freedom <em>around</em> them, not from negotiating <em>them</em>.
        </p>
      </Callout>
    </div>
  </section>

  <PageNext
    eyebrow="§ · Continue"
    title="Install — the deferred path, and what to read meanwhile."
    hint="Coming soon when the repo flips public. Until then, six pages of substantive content to walk."
    href="/install"
    tone="warn"
  />
</Layout>

<style>
  .page-head { padding: var(--m-space-16) 0 var(--m-space-8); }
  .lede { font-size: 17px; color: var(--m-fg-0); max-width: 70ch; }
  .dim-h1 { color: var(--m-fg-3); font-weight: 400; }

  .cl-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: var(--m-space-5);
    border-top: 1px solid var(--m-line-soft);
    padding-top: var(--m-space-5);
    margin: var(--m-space-6) 0 var(--m-space-5);
    max-width: 720px;
  }
  .cl-stats div { display: flex; flex-direction: column; gap: 4px; }
  .cl-stats dt {
    font-family: var(--m-font-mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--m-fg-3);
  }
  .cl-stats dd {
    margin: 0;
    font-family: var(--m-font-cond);
    font-weight: 600;
    font-size: 28px;
    color: var(--m-accent);
    line-height: 1;
  }

  /* Legend */
  .cl-legend {
    display: flex;
    gap: var(--m-space-4);
    flex-wrap: wrap;
    list-style: none;
    padding: 0;
    margin: 0;
    font-family: var(--m-font-mono);
    font-size: 11px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--m-fg-3);
  }
  .cl-legend li { display: flex; align-items: center; gap: 6px; }
  .cl-dot {
    width: 10px;
    height: 10px;
    border-radius: 2px;
    border: 1px solid currentColor;
    background: currentColor;
    flex-shrink: 0;
  }
  .cl-dot--brand    { color: var(--m-brand); }
  .cl-dot--accent   { color: var(--m-accent); }
  .cl-dot--danger   { color: var(--m-danger); }
  .cl-dot--accent-2 { color: var(--m-accent-2); }
  .cl-dot--warn     { color: var(--m-warn); }

  /* Two-col layout with sticky TOC */
  .cl-layout {
    display: grid;
    grid-template-columns: 1fr 260px;
    gap: var(--m-space-6);
    align-items: start;
  }
  @media (max-width: 920px) {
    .cl-layout { grid-template-columns: 1fr; }
    .cl-layout > :global(.toc) { display: none; }
  }
  .cl-releases { display: flex; flex-direction: column; gap: var(--m-space-4); min-width: 0; }

  /* Release card */
  .cl-release {
    background: var(--m-bg-2);
    border: 1px solid var(--m-line);
    border-left: 2px solid var(--m-fg-4);
    border-radius: var(--m-radius);
    padding: var(--m-space-5) var(--m-space-6);
    scroll-margin-top: 80px;
  }
  .cl-release--brand    { border-left-color: var(--m-brand); }
  .cl-release--accent   { border-left-color: var(--m-accent); }
  .cl-release--danger   { border-left-color: var(--m-danger); }
  .cl-release--accent-2 { border-left-color: var(--m-accent-2); }
  .cl-release--warn     { border-left-color: var(--m-warn); }

  .cl-head { margin-bottom: var(--m-space-3); }
  .cl-head-row {
    display: flex;
    align-items: baseline;
    gap: var(--m-space-3);
    flex-wrap: wrap;
    margin-bottom: 6px;
  }
  .cl-version {
    font-family: var(--m-font-mono);
    font-size: 18px;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--m-fg-0);
  }
  .cl-release--brand    .cl-version { color: var(--m-brand); }
  .cl-release--accent   .cl-version { color: var(--m-accent); }
  .cl-release--danger   .cl-version { color: var(--m-danger); }
  .cl-release--accent-2 .cl-version { color: var(--m-accent-2); }
  .cl-release--warn     .cl-version { color: var(--m-warn); }

  .cl-type {
    font-family: var(--m-font-mono);
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    padding: 2px 8px;
    border: 1px solid var(--m-line);
    border-radius: var(--m-radius-pill);
    color: var(--m-fg-3);
  }
  .cl-type--brand    { color: var(--m-brand);    border-color: rgba(240, 78, 35, 0.4); }
  .cl-type--accent   { color: var(--m-accent);   border-color: var(--m-accent-dim); }
  .cl-type--danger   { color: var(--m-danger);   border-color: var(--m-danger-border); }
  .cl-type--accent-2 { color: var(--m-accent-2); border-color: rgba(86, 184, 255, 0.4); }
  .cl-type--warn     { color: var(--m-warn);     border-color: var(--m-warn-border); }

  .cl-date {
    margin-left: auto;
    font-family: var(--m-font-mono);
    font-size: 11px;
    color: var(--m-fg-3);
    letter-spacing: 0.04em;
  }

  .cl-headline {
    margin: 0;
    text-transform: none;
    letter-spacing: 0;
    font-size: 20px;
    color: var(--m-fg-0);
    font-family: var(--m-font-cond);
    font-weight: 600;
  }

  .cl-highlights {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 6px;
  }
  .cl-highlights li {
    position: relative;
    padding-left: 22px;
    color: var(--m-fg-1);
    font-size: 13.5px;
    line-height: 1.55;
  }
  .cl-highlights li::before {
    content: '◦';
    position: absolute;
    left: 0;
    color: var(--m-accent);
    font-family: var(--m-font-mono);
  }
  .cl-release--brand    .cl-highlights li::before { color: var(--m-brand); }
  .cl-release--danger   .cl-highlights li::before { color: var(--m-danger); }
  .cl-release--accent-2 .cl-highlights li::before { color: var(--m-accent-2); }
  .cl-release--warn     .cl-highlights li::before { color: var(--m-warn); }

  .cl-summary {
    margin: 0;
    color: var(--m-fg-1);
    font-size: 13.5px;
    line-height: 1.6;
  }

  .cl-note {
    margin: var(--m-space-3) 0 0;
    padding: var(--m-space-3) var(--m-space-4);
    background: var(--m-bg-1);
    border-left: 2px solid var(--m-fg-4);
    border-radius: var(--m-radius-sm);
    font-size: 13px;
    color: var(--m-fg-2);
    font-style: italic;
    max-width: none;
  }
  .cl-release--brand    .cl-note { border-left-color: var(--m-brand); }
  .cl-release--accent   .cl-note { border-left-color: var(--m-accent); }
  .cl-release--danger   .cl-note { border-left-color: var(--m-danger); }
  .cl-release--accent-2 .cl-note { border-left-color: var(--m-accent-2); }
  .cl-release--warn     .cl-note { border-left-color: var(--m-warn); }
</style>
