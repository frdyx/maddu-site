// releases.ts — single source for the changelog release list. Mirrored
// from src/pages/changelog.astro / atom.xml.ts so feeds, the API, and
// the page never drift. When a new release ships, update this file
// and the change propagates to /changelog, /atom.xml, /feed.json, and
// /api/changelog.json in one stroke.

export type ReleaseType = 'milestone' | 'feature' | 'hardening' | 'polish' | 'patch';

export interface Release {
  v: string;
  id: string;
  date: string;             // YYYY-MM-DD
  type: ReleaseType;
  headline: string;
  summary: string;
}

export const RELEASES: Release[] = [
  { v: 'v1.115.0', id: 'v1-115-0', date: '2026-08-05', type: 'milestone', headline: 'Memory-recall track — trust-gated recall: no unreviewed fact reaches an agent (+ Máddu as an MCP server)', summary: 'The memory-recall track lands whole (diff Codex-CLEAN after 10 review rounds — 36 findings fixed: 7 blocker, 20 major, 9 minor; contract 1.11.0 → 1.13.0). The defects were honest ones: CLI slice-stops ran hindsight but never the wiki (462 of 726 live stops missing at fix time), cross-corpus search was substring + timestamp so recency always beat relevance, and 726 of 935 facts were auto-promoted regex summaries — feeding memory back to agents would have amplified unreviewed agent assertions. Now one slice-stop materializer serves both transports (idempotent wiki append — replays are byte-identical no-ops), and a new maddu wiki verb ships sync/drift/page/list/rebuild: the live backfill appended 462 missing blocks, drift → 0. Search gets BM25 relevance (golden suite Recall@5 1.000, MRR 0.96, p95 6ms) with IDF computed over the full per-kind corpus so it stays honest. Fact trust is event-sourced: every fact starts asserted — searchable, never injectable; memory approve binds an approval to a sha256 of the WHOLE canonical fact (any field edit voids it); revoke requires a reason; trust survives rebuild (spine wins). The recall packet is a single eligibility seam — approved-only, ranked, hard-capped at 8 items / 16KB with complete per-reason withheld accounting; brief --for-agent re-derives trust immediately before witnessing, records MEMORY_INJECTED with per-fact {id, sha256, approvalEvent} rows, and FAILS CLOSED on any error — fact text renders witnessed or not at all. A new critical gate memory-injection-bounded (gates 74 → 75) folds the UNCAPPED history: bounds + approval-reference validation hard-fail; temporal anomalies a merged order cannot prove causal surface as named WARNs — detected, never silent. And maddu mcp serve (verbs 72 → 73) makes Máddu an MCP server — hand-rolled JSON-RPC 2.0 over stdio, Node stdlib only: memory_search, recall_packet (inspection ≠ injection: no witness), wiki_read, atlas_query, status; a conformance fixture hash-proves spine + memory + wiki untouched by a full dialogue. Self-test 189/189; ~250 new assertions across 9 suites.' },
  { v: 'v1.114.0', id: 'v1-114-0', date: '2026-07-24', type: 'hardening', headline: 'Worktree two-resource recovery — a crash mid-detach can no longer strand a lane', summary: 'PR-D of the session-lifecycle track (diff Codex-CLEAN after 6 review rounds; contract 1.10.0 → 1.11.0). The defect: detachLaneWorktree removed the git checkout BEFORE appending the terminal WORKTREE_DETACHED, so a crash between stranded the lane forever — the attachment folds as LIVE (a pure spine fold, no fs check), release is blocked, a retry throws, and nothing self-heals; hard rule #2 forbids a rollback-atomic write across the two resources. Detach is now intent-first: a durable WORKTREE_DETACHING lands after dirty/ancestry authorization and before git removal, so a crash is recoverable from the intent — never by inferring “gone” from filesystem absence (a storage outage is indistinguishable). Every attach mints a per-worktree instance token into the checkout’s private git dir, so recovery removes only a token-matched PRESENT instance — a manually-replaced checkout is never deleted. Auto-finalize is positive-removal-only inside the janitor’s rule-#9 gauntlet; an absent instance is never auto-terminalized — it surfaces needsOperator with the exact lane release --worktree --recover command per case. Operator recovery authorizes via an active-owner-aware matrix (present+match → remove + terminalize; present+mismatch → orphaned leftover, never removed; foreign origin → refuse + redirect — a terminal synced back would close the source replica’s real attachment), recording both recoveryActor and attachmentOwner. A new atomic-publish lock serializes every worktree writer in disjoint namespaces; dead owners are stolen by atomic quarantine. New suites: worktree-lock 26, recovery 42, recovery-fold 17, writer census 9; self-test 185/185.' },
  { v: 'v1.113.0', id: 'v1-113-0', date: '2026-07-23', type: 'hardening', headline: 'Lane-writer serialization — and the bridge release authorization hole closed', summary: 'PR-C of the session-lifecycle track (diff Codex-CLEAN after 5 review rounds; no new event types, project() output byte-identical, golden-tested). The headline fix: POST /bridge/lanes/release previously appended LANE_RELEASED for ANY sessionId with no holder check — in default mode a release clears the lane, so any caller could evict any lane’s real holder. Release now requires the actor to OWN the lane (404 no-claim / 409 owned-by-another, holder intact), and worktree disposition authorizes by the attachment owner, not the claim holder, so a superseded owner can neither orphan its own live checkout nor be blocked from withdrawing. Every LANE_CLAIMED / LANE_RELEASED / LANE_CLAIM_FORCED mutation now decides AND appends inside one transaction, against a fresh strict snapshot taken under a new lane-claims lock — spine.append serializes only the physical write, never the decision window above it, which is exactly the race this closes. A lane-writer census tripwire fails if any raw ownership append reappears outside the helper. Claim requires an active REGISTERED session and refuses on any active rival owner; force is the sole evictor, stamping an import-stable forceGroup id on the whole bundle; the force-discipline gate reconstructs each bundle and hard-fails a forged prior — and in team-sync mode the reconstruction is withheld as unsound (a merged replica history can resurrect an earlier first-claimer), stated honestly rather than faked. Deliberate migrations: lane claim refuses an unregistered or closed session; the auto-claim fallback lane is now auto/<full-session-id>. Concurrency matrix 48/48; self-test 181/181.' },
  { v: 'v1.112.0', id: 'v1-112-0', date: '2026-07-22', type: 'hardening', headline: 'Session-id validation at the chokepoints', summary: 'PR-B of the session-lifecycle track (diff Codex-CLEAN after 4 review rounds) — the id-validation campaign the PR-A review surfaced, split out as a seeded follow-up. One id grammar (id-grammar.mjs), enforced at every ingress that turns caller input into a sessionId — the bridge body reader, CLI --session / MADDU_SESSION_ID resolution, register, heartbeat/close, and the lane-ownership entry points — so a malformed id is refused at the boundary, never interpolated into a projection key or an event. A sid-surface-census tripwire enumerates every place a sessionId crosses from untrusted input into the trusted core and asserts each one routes through the validator, so a new ingress can’t silently reintroduce the gap. A missing session file (ENOENT) stays honestly distinct from a malformed id (refused). No contract change beyond the stricter ingress validation.' },
  { v: 'v1.111.0', id: 'v1-111-0', date: '2026-07-22', type: 'hardening', headline: 'Session-lifecycle serialization + honest dirty-age — six shipped bugs fixed', summary: 'PR-A of the session-lifecycle track (plan v27, Codex-SOUND after 27 review rounds). Every lifecycle mutation now goes through serialized, condition-checked primitives: ONE conditional close shared by CLI/bridge/hooks/janitor (already-closed → no duplicate event; never-registered → no verify-invalid append; an in-lock precondition means the janitor can never close a just-heartbeated session) and atomic registration with duplicate rejection in ANY status — a closed session can no longer be resurrected by a same-id re-register. SessionEnd closes ITS OWN session, resolved via the SessionStart binding — an unbound end closes nothing. Six pre-existing shipped bugs fixed along the way: a renewed session stayed visibly stale forever; git rename records mis-parsed the source path as a bogus dirty path; the auto-close note was silently discarded; a hook bootstrap failure could block an edit or compaction (all four hooks now bootstrap inside fail-open containment); closing a missing id appended a verify-invalid event; a baseline-dirty path was excluded from dirty counting FOREVER even after going clean and re-dirtying. Dirty-age got honest: scratch-glob ignores, per-file first-seen clocks (age = the oldest still-dirty file; committing it drops the clock), staged-rename clock transfer, and failed observations suppress the commit gate and report dirty=unknown instead of a false clean. No contract change; new session-lifecycle suite (55 asserts), discipline suite grown to 112.' },
  { v: 'v1.110.0', id: 'v1-110-0', date: '2026-07-21', type: 'milestone', headline: 'spine anchor --assess — the consume ritual; the assurance ladder is complete', summary: 'PR 6a of the verification-witness track — the shippable ladder is COMPLETE: actor-reported → replayed → anchored (presence-attested stays deferred, and that is stated, not hidden). An interactive operator ceremony records ASSURANCE_ASSESSED as a non-authoritative ledger note — labeled that way by every consumer, --json carries authoritative: false. The tool’s checks can only REFUSE, never grant the level: payload↔receipt↔SHA binding with symlink-free containment, full anchor-chain verification (mid-history deletion, prev-chain breaks, forged ANCHOR_* events), witness.maxAnchorAge staleness, and a TOCTOU recheck run AFTER the final confirm — anything that moved during the operator’s verification window refuses with nothing recorded. The positive evidence is the operator’s own external Bitcoin-backed ots verify run, pasted verbatim (redacted BEFORE the 8 KiB cap so a token straddling the boundary can’t evade the scrubber) and confirmed explicitly (default No). No verifier is ever executed by the tool — a runner the agent controls re-verifying its own evidence would be actor-as-witness. Honest residuals on every surface: only the newest anchor’s proof digest is disk-checkable; suffix deletion of anchors still needs a retained operator checkpoint; an anchor proves a receipt existed by a time — not that the work was good. Threat model gains scenario 13 (the ladder + its stated residuals); SECURITY.md’s witness claim updated — the “planned, not shipped” placeholder is gone. 75-assert kill-criteria suite; plan Codex-SOUND over 9 rounds, diff CLEAN in 3. Verbs 72; gates 74; no contract change.' },
  { v: 'v1.109.0', id: 'v1-109-0', date: '2026-07-21', type: 'feature', headline: 'spine verify --replay — the receipt re-earned in a clean checkout, not re-asserted', summary: 'PR 5 of the verification-witness track (plan Codex-SOUND after 5 rounds). The replayed rung gets its producer: clone the repo at an EXACT commit and re-run the verification the repo ITSELF declares — maddu.json → replay:{install?, verify}, read from the git object store AT the subject SHA, so a dirty worktree can never choose the commands for an older commit; a commit predating the declaration is honestly unsupported (exit 2, no spine events), and no lockfile/ecosystem inference exists anywhere in the module. Strict receipt discipline: setup refusals emit nothing; a STARTED append failure refuses pre-run (replay will not run unrecorded); a RAN append failure exits nonzero and never claims replayed. Settlement-proof execution: each declared command gets a fixed 10-minute timeout with whole-process-tree kill under a settlement deadline that starts at kill initiation — a stalled kill can never hang; a spawn error settles as a fail, never a dangling STARTED. Fail-closed cleanup: the clone is deleted BEFORE the receipt, and a cleanup failure forces fail + nonzero exit even when the commands passed — an incompletely executed replay protocol never reads as a successful replayed run. Honest scope, printed every run: clean-checkout reproducibility + dirty-worktree contamination detection; host env, credentials, caches, and services are NOT isolated. New docs/55; suite 41/41 including the Windows grandchild tree-kill. Verbs 72; gates 74; no contract change.' },
  { v: 'v1.108.0', id: 'v1-108-0', date: '2026-07-21', type: 'feature', headline: 'spine anchor — the external witness (OpenTimestamps → Bitcoin)', summary: 'PR 4 of the verification-witness track — the first evidence that lives OUTSIDE the agent’s authority. maddu spine anchor stamps a canonical payload committing to a verification receipt AND the broader history (receipt digest, spine position, chain head, prev-anchor hash, seq) into Bitcoin via the stock OpenTimestamps client — a declared ambient tool like git, zero new package dependencies, ~1s of anonymous HTTP, no accounts. --upgrade completes proofs after Bitcoin confirmation (dogfooded: block 958943); --verify is a read-only self-consistency diagnostic that is NEVER assurance evidence — and Bitcoin-backed verification is deliberately an OPERATOR action (ots verify, local node or explorer mode with its reduced trust stated), not a maddu verb, because a runner the agent controls re-verifying its own evidence is actor-as-witness. Contract 1.9.0 → 1.10.0 (ANCHOR_STAMPED, ANCHOR_UPGRADED, ASSURANCE_ASSESSED + validateAssuranceEvidence). No offline queue — offline says so and exits nonzero with nothing recorded; concurrent invocations serialize under a funnel lock; team-sync mode is refused fail-closed (a singular chain head cannot cover a merged multi-replica spine). Adversarial --verify FAIL-flags mid-history renumbering/deletion, edited payloads, rewritten spine positions, and forged ANCHOR_* events — while restating every time that suffix/all-history deletion is NOT detectable without a retained operator checkpoint. An anchor proves the receipt EXISTED by a time, not that it is true — content honesty is replay + the ceremony. .maddu/anchors/ is tracked, so anchors travel with the repo. Windows works with the stock client v0.7.2 (documented DLL shim). New docs/54; suite 36/36.' },
  { v: 'v1.107.0', id: 'v1-107-0', date: '2026-07-20', type: 'hardening', headline: 'Consumer permission guardrails ride hooks install', summary: 'PR 3 of the verification-witness track. The guardrails that previously existed only as hand-authored settings on the maintainer workstation now ship to every consumer by default: maddu hooks install merges Edit-form deny rules over framework internals (maddu/runtime/**, .maddu/config/**, .maddu/gates/**, both settings files) into the consumer’s .claude/settings.json, plus ask rules generated from maddu.json → guardrails.ask[] — declared by the project, never guessed; --no-guardrails opts out. Recorded ownership with preservation proven end-to-end: the exact strings an install adds are recorded, uninstall strips exactly that set, and e2e drives the real CLI — install → byte-identical second install → uninstall leaves user-authored content intact. The honest framing ships in the CLI output itself: bypassable harness friction covering the built-in file tools, NOT a security boundary — Bash coverage is version-dependent, and subprocesses are never covered (bypass demonstrated and documented). Eight Codex review findings applied pre-merge across three hardening rounds: a lost ownership record can no longer turn uninstall into a silent no-op that leaves every guardrail behind; malformed permission shapes refuse the install (file untouched) instead of writing rules that vanish at serialize time or clobber user data; user pre-existing empty arrays survive byte-identical. Unit 76/76, e2e 35/35. Verbs 72; gates 74; no contract change.' },
  { v: 'v1.106.0', id: 'v1-106-0', date: '2026-07-20', type: 'hardening', headline: 'Verdict-machinery drift detection — the record tells you when the yardstick moved', summary: 'PR 2 of the verification-witness track (PR 1 was the docs-honesty pass). The unshipped oracle-pin branch is descoped to its PROVEN fixes and renamed to what it honestly is: verdict-machinery drift detection — it watches the machinery that decides what a result MEANS; test assertions ARE the oracle and are not covered. Kept, Codex-verified: CRLF-normalizing content hashing (raw-byte hashing broke every Windows autocrlf checkout), binary raw-byte preservation, full-replacement projection semantics, and regression tests proven to fail on pre-fix code. The tracked-source-drift gate is demoted critical → warn and removed from the required set — a cooperative “the yardstick moved” signal, deliberately NEVER an assurance input: an actor who can edit the pinned files can re-pin them, so critical falsely lent it trust-boundary weight. No reasonless re-pin: maddu sources rebuild refuses an absent reason and a flag-shaped or whitespace-only one, and the --force waiver is gone — contract 1.8.0 → 1.9.0 lists reason + by on SOURCE_HASH_RECOMPUTED. An empty pin config is never green anywhere on the surface. Threat-model §12 rewritten + SECURITY.md aligned — four false claims fixed. Fresh clones go green with an honest “nothing pinned yet” WARN. Verbs 72; gates 74.' },
  { v: 'v1.105.2', id: 'v1-105-2', date: '2026-07-19', type: 'patch', headline: 'ci-command test no longer clobbers the real ci.json', summary: 'Test-isolation leak found while shipping v1.105.1: the ci-command test ran maddu ci against the REAL repo root and mutated .maddu/config/ci.json in place, restoring in a finally — so a harness kill on timeout, or a concurrent gate reading mid-run, leaked the clobbered single-gate state into the working tree, dropping the 52-gate required set to 1. A new MADDU_CI_PROFILE env seam (test/CI-only) redirects both reads and writes to an absolute override path; the override read is fail-closed — only ENOENT means unpinned, a corrupt or wrong-shaped override exits nonzero with a diagnostic, never a silent green. The test is now fully isolated and asserts the real ci.json is byte-identical (existence + bytes) before and after the run. ci-command 21/21; no contract change.' },
  { v: 'v1.105.1', id: 'v1-105-1', date: '2026-07-19', type: 'patch', headline: 'Atomic Claude→Máddu session binding — a consumer incident, root-caused', summary: 'Surfaced by a consumer repo upgrading Máddu mid-session: the discipline gate blocked a legitimately-running session with “no active session governs.” The load-bearing bug: the claude→maddu binding write was an UNLOCKED read-modify-write — two concurrent SessionStarts (the long-lived bridge + a CLI invocation) could clobber each other’s entries — and the handler re-read the SHARED active-session pointer for the sid to bind, so both Claude ids could bind to ONE Máddu session. Fixed: binding serializes under the existing per-repo advisory lock with a finite 2s wait (a stuck holder times out rather than hanging session start); writeJson is atomic temp+rename so a lock-free reader never sees a torn map; maddu register now returns the session id it CREATED and SessionStart binds that — never the shared pointer. The honest remedy is documented: an unbound running session cannot be healed by maddu register (the Claude session_id reaches Máddu only on the hook’s stdin) — restart the session so a fresh SessionStart binds it. Deliberately NOT done, Codex ruled it unsound over two rounds: auto-adopting an existing session at edit time — an unbound caller can’t be authenticated as the owner. Full self-test green.' },
  { v: 'v1.105.0', id: 'v1-105-0', date: '2026-07-16', type: 'feature', headline: 'Usage-audit Tier 5 (final) — learn as a measured trial at the ritual boundary', summary: 'The roadmap’s last tier — the 2026-07-16 usage-audit roadmap is fully drained. The audit found maddu learn self-dev-only: 11 accepted corrections in the framework repo, 0 across all 21 consumers — the verbs exist; nothing surfaced them where the work happens. Now slice-stop (and session close) run a READ-ONLY learn candidate detection pass and preview what maddu learn would harvest — accept one-liners point at the existing verbs; nothing is auto-written, so no rule-#9 gauntlet entry. The containment contract is proven through the real CLI: post-append try/catch isolation (a deliberately-throwing detector still stops green), a bounded session window (500 lines / 256KB), and a 1500ms cooperative deadline (a slow detector never stalls the ritual). No new heuristics — detection reuses the existing deterministic learn-spine miners. And it ships as a measured trial with a pre-authorized demotion: adoption metric = accepted corrections, a 4-week window with a 12-week hard backstop (a cohort that never forms IS the verdict); on failure a binding demotion PR removes the hook-in and reclassifies learn as expert/self-dev. The considered LEARN_RETRIEVED contract bump was not taken — recorded, not silent. Verbs 72; gates 74; no new event type.' },
  { v: 'v1.104.0', id: 'v1-104-0', date: '2026-07-16', type: 'feature', headline: 'Usage-audit Tier 4b — minimal default catalog + the init --force data-loss fix', summary: 'New installs stop prescribing a lane taxonomy: a fresh maddu init seeds a single general lane — the claim-anything fallback — and the catalog grows from observed reality via Tier 4a’s lane suggest --adopt. (A deliberate deviation from the audit’s “empty catalog”: one claimable lane must exist for the discipline/claim --worktree flows.) Existing installs untouched — and the review made that actually true: it caught that init --force had been unconditionally overwriting catalog.json, wiping claims.json, and truncating the spine’s first event segment — a long-standing, pre-existing data-loss path. All three fixed: --force re-installs framework files only, records the re-install through the canonical spine.append, and spine verify passes afterward. Compat evidence: a real end-to-end init → hooks → claim → slice-stop → release → ad-hoc-claim ritual passes on the minimal catalog (22-check suite). Verbs 72; gates 74; no new event type.' },
  { v: 'v1.103.0', id: 'v1-103-0', date: '2026-07-16', type: 'feature', headline: 'Usage-audit Tier 4a — lane catalog: observed, not prescribed', summary: 'The default 7-lane catalog measured 76% dead fleet-wide (112/147 placements never claimed) while 64% of consumer claims were ad-hoc — the catalog prescribes and reality routes around it. maddu lane suggest (a new subcommand on the existing verb) compares the catalog against lifetime observed claims: dead entries flagged, suggestions from claim counts ONLY (≥3 lifetime claims of the same non-ephemeral ad-hoc id; auto-/numeric ids and imported backfill never count; repo-structure inference explicitly dropped). Suggestions only — the operator confirms. --adopt graduates a suggestion into the catalog and wakes the dormant LANE_ADDED; --prune removes a never-claimed entry (refusing anything ever claimed), waking LANE_REMOVED. The mandated schema verification found real drift: the bridge’s LANE_REMOVED emitter wrote {} against the documented { ok: boolean } — both emitters now conform, the shape pinned by self-test. lane list marks never-claimed entries (unused); insights lanes reproduces the audit’s dead-catalog table fleet-wide. Two dormant event types wake; none added. Verbs 72; gates 74.' },
  { v: 'v1.102.0', id: 'v1-102-0', date: '2026-07-16', type: 'feature', headline: 'Usage-audit Tier 3 — activation funnel', summary: 'The audit found 5/21 installs (24%) parked at “installed, never started the ritual” — thousands of passive events, zero sessions/claims/slice-stops — with nothing in the product saying so. The activation funnel is a read-only spine derivation of the lifetime furthest ritual stage — installed → healthy → session → claimed → slice → repeating (≥3 slice-stops) — monotonic, with passive gate/doctor/boot traffic and imported backfill explicitly never counting as adoption. maddu doctor prints the stage + ONE next action (the healthy→session action points at maddu hooks install — the proven activation lever). maddu fleet gains a funnel column + a headline rollup flagging parked installs; upgrade nudges when session discipline is unwired; init says an unhooked install stays parked. Live acceptance: the funnel matched the audit’s hand-derived segmentation for all 22 workspaces. Verbs 72; gates 74; no new event type.' },
  { v: 'v1.101.0', id: 'v1-101-0', date: '2026-07-16', type: 'feature', headline: 'Usage-audit Tier 2 — execution telemetry (invocation receipts)', summary: 'Record what actually runs, not what gets mentioned — the audit’s verb stats were transcript keyword mentions. Every maddu entry now appends one CLI invocation receipt (ts/verb/sub/exit/ms/sessionId/workspace) to a new dedicated corpus .maddu/state/invocation-receipts.ndjson — deliberately NOT the spine (telemetry isn’t a fact the record must witness), fail-open at every step (nothing ever blocks or noises a verb; p50 write <10ms), secret-scrubbed at the write boundary, and device-local (never chained, synced, or exported). Honest incompleteness by contract: receipts are an observed-window signal — every surface renders the retention window + dropped-line count, and none may present receipt counts as lifetime totals; declared by the new maddu log --window. insights verbs is receipts-first; the transcript scan is demoted to a labeled legacy mentions signal. Gate outcome discrimination: non-pass GATE_RAN states enumerated + a fleet Gate-outcomes rollup + a new gate-fail-path self-test proving failing gates land on the spine — the first fleet run showed the audit’s “0 fail” was the collector misreading the payload (real fleet: 68 warn + 50 fail; the fail path was writing all along). No new event type, no contract bump. Verbs 72; gates 74.' },
  { v: 'v1.100.0', id: 'v1-100-0', date: '2026-07-16', type: 'hardening', headline: 'Usage-audit Tier 1 — instrument truth', summary: 'First tier of the 2026-07-16 fleet usage-audit roadmap (a 22-workspace spine audit that caught the instruments lying): before changing what the instruments measure, make them honest. Version SSOT (installed-version.mjs): one resolver shared by maddu fleet AND maddu insights — the audit caught them disagreeing 1.15.0 vs 0.19.0 on the same repo, because insights replayed the spine’s install-time version that upgrades never touch — both now agree 22/22 and print the source they resolved from. Import/native segmentation keyed on data.source (not event type): imported transcript backfill no longer moves activity/recency/volume (one repo drops from a misleading 55,225 events to its native 1,557); --include-imported restores merged totals with imported tallies reported apart. Exhaustive taxonomy partition: every one of the 182 event types lands in exactly one of fired / imported-only / dormant-by-design / plugin-owned / dead, and the buckets must sum to 182 (self-tested). Role segmentation: insights --role consumer|fixture|self, so framework self-development can’t masquerade as fleet adoption. SKILL_INJECTION_REFUSED reclassified dormant-by-design (a security refusal witness whose silence is the good outcome) → fleet dead count 0. Event contract 1.7.0 → 1.8.0 (MINOR: INBOX_MESSAGE/SKILL_CREATED/SKILL_UPDATED gain optional source). Verbs 72; gates 74.' },
  { v: 'v1.99.0', id: 'v1-99-0', date: '2026-07-10', type: 'hardening', headline: 'Audit remediation — secret-scrub write-boundary + doc-drift (P5, the final tier)', summary: 'The last tier of the 2026-07-09 flaw audit — the remediation roadmap is now fully drained (6/6 tiers). The “secrets are redacted at the write boundary” guarantee was aspirational: the redactor covered the spine, but ~10 auxiliary state stores wrote caller/agent/subprocess text raw, the detector missed several common secret shapes, and a second stale scanner had drifted. One canonical detector (secret-scan.mjs) now recognises GitHub fine-grained (github_pat_) + user-to-server (ghu_) + refresh (ghr_), Google (AIza…), Stripe live/test secret+restricted keys, and a correct PEM private-key matcher (complete-label backreference block + an unterminated-to-EOF fallback so a truncated key body can’t survive); field-name awareness adds refresh_token / client_secret / bearer (bare token/secret deliberately excluded — the key match is substring-based and would eat sessionToken). Every discrete state-store write is now scrubbed at the boundary via a new value-pattern redactLeaves (non-breaking on config field names): checkpoint tag message + index, active-session, schedules (repo + global), review archives, memory facts, MCP/runtime descriptors + health, and the lane catalog; the importer routes through the same detector. Documented residual, stated plainly in SECURITY.md + the threat model: worker subprocess stdout/stderr log files are fd-direct streams and are not auto-redacted (a correct fix needs a chunk-boundary-safe streaming redactor). Doc/count drift reconciled too — version.json/package.json/package-lock to 1.99.0 (pinned by a new version-parity self-test), the governance gate count (10→74), and the README landing page (contract 1.7.0 / 182 types). No new gate/verb/event type — the new checks are auto-discovered self-tests. Plan + diff Codex-reviewed; Actions green under the required ci check.' },
  { v: 'v1.98.0', id: 'v1-98-0', date: '2026-07-10', type: 'milestone', headline: 'Audit remediation — the spine becomes tamper-detecting, and no agent is the sole witness to its own work (P0–P4)', summary: 'A 2026-07-09 seven-auditor flaw audit (Codex-verified) found several headline guarantees passing for the wrong reason — two systemic patterns: green-because-wrong-scope (a gate scans a path that misses the real code) and actor-is-witness (a gate re-reads a record the actor’s own process wrote). Five tiers, each shipping the gate that would have caught its own bug. P0a (#266) closes a cockpit stored-XSS — the mailbox subject + five defense sinks escape to text nodes; markdown safeHref rejects javascript:/control-char bypasses. P0b (#267) adds bridge capability-token auth — a per-boot timing-safe secret gates every mutating route and any cross-workspace request (BRIDGE_CROSS_WORKSPACE); honestly a loopback-CSRF boundary, not inter-process auth. P1 (#268) makes the spine tamper-detecting: a post-cutover chain FAILs spine verify on an interior edit, deletion, insertion, reorder, or prev_hash-strip, backed by one funnel-locked flat-append primitive so concurrent writers can’t fork — unkeyed, so a determined actor who recomputes the whole chain stays out of scope (the OS’s job); the claim is reworded from evident to detecting throughout. P2 (#269) makes discipline honest — a 5-class Bash classifier where WRITE dominates self-disable, DISCIPLINE_SKIPPED/ENFORCEMENT_ERROR witnesses on every fail-open, and a guarded governance off-switch (domain-checked, rank-weakening, write-ahead GOVERNANCE_OVERRIDE_CHANGED). P3 (#270) replaces actor-witness with verification: success-eval, test-recency, and completion-claim now believe a tamper-detecting VERIFICATION_STARTED/RAN spine receipt from the runner’s in-process result, never a hand-writable state file. P4 (#271) fixes gate scope + CI integrity — rule-5 scanned a nonexistent path (0 files → green) now scans the real tree with a lexically-aware banned-import matcher, SKIP-as-PASS is closed (reserved exit 77 + --fail-on-skip), warn-only “required” gates were repinned fail-capable (66→52), and branch protection is ON with a required aggregate ci check on main. Event contract 1.7.0, 182 typed event types; verbs 72; gates 74. Each tier’s plan and diff adversarially reviewed by Codex before self-merge.' },
  { v: 'v1.97.0', id: 'v1-97-0', date: '2026-07-09', type: 'milestone', headline: 'Máddu’s governance stopped being advisory — self-enforcing discipline + the Operator Plane', summary: 'Thirteen PRs (#251–#263) across four arcs, the enforcement work adversarially reviewed by Codex over three rounds, without touching the deterministic default path. The Claude Code PreToolUse hook now hard-blocks a mutating edit (Edit/Write/MultiEdit/NotebookEdit + classified Bash writes) when a session ritual is stale — no session, no lane, no governing goal/plan, an overdue slice-stop, or an uncommitted pileup. Tier-scaled by governance (strict blocks, standard is graduated warn→block, relaxed nudges), fails open (any error → allow; the hook never exits 2), and never gates the remedy (maddu slice-stop / goal set / plan / lane claim, git commit) so a block can’t deadlock its own fix. Plus gates-before-done (goal done / plan complete refuse at strict while a required gate is red) and the discipline-observed doctor gate. Alongside it, the Operator Plane: a read-only readout for the human who can’t read the code — maddu status --line, a “while you were away” digest, a single-project cockpit, a decision ledger whose every row-sha ties to the tamper chain, an enriched handoff, and a cross-workspace portfolio wall with “needs-the-human”. The oversight surface shows what the agent did with a skill — fed vs withheld and the plain-language why. Event contract 1.3.0, 175 typed event types; verbs 72; gates 74; cockpit routes 46→50.' },
  { v: 'v1.96.0', id: 'v1-96-0', date: '2026-07-07', type: 'hardening', headline: 'Central write-boundary payload redaction — the record you share can’t leak a pasted secret', summary: 'Every event’s data is routed through the canonical redactor inside spine.append before the line is built and hashed — one choke point covers every emit site, present and future. Clean data (the normal case) passes through by reference: 0 clones, 0 byte changes across 2,964 real events. A new key-aware rule redacts a long token under a sensitive key name (password, apiKey, …) that a value-only or line-wise scan structurally misses. Redaction is write-time only; the export and team-sync refuse-gates remain the defense for history. Event contract untouched at 1.1.0.' },
  { v: 'v1.95.0', id: 'v1-95-0', date: '2026-07-06', type: 'feature',   headline: 'SLM-factory governance — maddu model, phases 0–6', summary: 'Máddu as the governance substrate around a domain-specific small-model factory: it records hash-pinned manifests, derives the promotion ladder from the spine, and gates every hand-off — but never trains, serves, or evaluates (zero new dependencies across the whole arc). maddu model covers dataset snapshot, train, checkpoint register, eval, promote/--confirm, release, and rollback, with a 12-gate ML-lifecycle starter pack you install and pin. Stage is derived from the spine: approvals raise it, rollbacks lower it, and the CLI cannot write a record spine verify would flag. Event contract 1.0.0 → 1.1.0 (11 MODEL_* types).' },
  { v: 'v1.94.0', id: 'v1-94-0', date: '2026-07-06', type: 'feature',   headline: 'Experience protocol & evolution planner — maddu experience / evolve', summary: 'The spine re-read as normalized experience, plus a recommend-only evolution planner — every path a pure read-time derivation with zero new event types and zero spine writes. maddu experience turns each event into a step whose id IS the source event id, grouped into trajectories by explicit session linkage only (the unlinkable lands in an honest env trajectory, never guessed). maddu evolve runs four deterministic detectors and recommends — never applies. maddu experience export --format atdp is the governed sharing boundary: a refuse-on-hit secret gate with no skip flag and deterministic, clock-free bytes.' },
  { v: 'v1.93.0', id: 'v1-93-0', date: '2026-07-05', type: 'feature',   headline: 'Worktree lanes — parallel agents, isolated checkouts, one spine', summary: 'maddu lane claim <id> --worktree provisions an isolated git worktree per lane, bound to the claim — parallel agents stop sharing a checkout while every event still lands on the primary spine (a work-root / state-root split makes a silently forked record impossible). Release is a verified disposition: lane release --worktree merged|abandoned|keep — merged checks the branch really is an ancestor of the integration ref and refuses to orphan un-integrated work. Máddu never runs your merge; it verifies you did. A new worktree-lane-coherence doctor gate names drifted worktrees — orphaned, wrong branch, dirty — before they bite.' },
  { v: 'v1.92.2', id: 'v1-92-2', date: '2026-07-04', type: 'patch',     headline: 'focus director: verbosity is not drift — the absolute-anchor metric (incident catch)', summary: 'The drift tagger divided goal-token overlap by the focus text’s size, so every detailed, honest, on-goal slice summary read as away (5/5 false positives on one day) while a terse "working on autonomy" would have passed. The new metric counts absolute anchors — whether the goal’s distinctive terms appear at all: ≥2 anchors → toward, 1 → lateral, 0 → away — with light stemming, and the goal axis narrows to objective + constraints. The regression cases are the incident itself; every tag now carries its anchors, so it’s self-explanatory.' },
  { v: 'v1.92.1', id: 'v1-92-1', date: '2026-07-03', type: 'polish',    headline: 'Cockpit surfaces earned autonomy', summary: 'AUTONOMY_SCORED / AUTONOMY_RECOMMENDATION events classify into the trust family with human summaries — a scored digest and rung-change arrows (backend: observe → relaxation-candidate) — and a live recommendation renders as its own Autonomy row on the orientation brief card, rebuilt from the spine via a new autonomy projection slot.' },
  { v: 'v1.92.0', id: 'v1-92-0', date: '2026-07-03', type: 'feature',   headline: 'Earned autonomy — maddu autonomy (recommend-only)', summary: 'maddu autonomy — a deterministic per-lane trust score over the verified record: a Wilson lower bound over witnessed-clean vs witnessed-dirty slice outcomes, a 3-rung ladder (observe / established / relaxation-candidate), and a daily clean-credit cap against deliverable-farming. Recommend-only by contract: nothing in the feature writes governance config — applying a recommendation is the operator running maddu governance set. Recommendations are muted while any phase is active; maddu orient and governance show announce the latest one next to the tier it informs.' },
  { v: 'v1.91.2', id: 'v1-91-2', date: '2026-07-03', type: 'hardening', headline: 'doctor watches the global binary (incident catch)', summary: 'A stale npm i -g maddu on PATH shadows a newer checkout and silently runs old behavior — surfaced live when a stale global demanded --session inside a current checkout while doctor stayed green. doctor now compares the running CLI’s version against the checkout’s version.json (older → WARN with the npm i -g remedy), and the consumer framework-version WARN is direction-aware instead of advising maddu upgrade both ways.' },
  { v: 'v1.91.1', id: 'v1-91-1', date: '2026-07-03', type: 'polish',    headline: 'Cockpit surfacing for the governance-arc domains', summary: 'The event stream renders COMPACTION_CHECKPOINT, vendor-memory facts, and the sterile-phase tier; the memory filter gains correction + vendor kinds; and the orientation brief card shows the sterile-phase line. (Backfilled entry — shipped without a changelog line.)' },
  { v: 'v1.91.0', id: 'v1-91-0', date: '2026-07-03', type: 'feature',   headline: 'Sterile phases — per-phase strictness (escalation-only)', summary: 'maddu phase set --name <n> --tier strict|standard|relaxed — while the phase is active, the effective governance mode is the stricter of workspace mode and phase tier. Escalation-only: a relaxed phase tier on a strict workspace changes nothing; weakening stays an explicit governance set. The base config is never rewritten — maddu phase clear lifts the escalation and is the whole rollback. Loops, the coordinator, strict-mode approvals, and governance show all resolve through the escalated view.' },
  { v: 'v1.90.0', id: 'v1-90-0', date: '2026-07-03', type: 'feature',   headline: 'Vendor-memory interop — learn sync --from-claude-memory', summary: 'maddu learn sync --from-claude-memory imports Claude Code’s default-on auto-memory as vendor facts with full provenance. Import-only by contract — the vendor directory is never written — and content-hash-deduped, so re-running is idempotent and an edited memory imports as a new fact. Preview by default, --adopt to write; each import rides a VENDOR_MEMORY_IMPORTED event so a memory rebuild replays faithfully.' },
  { v: 'v1.89.1', id: 'v1-89-1', date: '2026-07-03', type: 'patch',     headline: 'Hook entrypoint resolved per repo layout (dogfood catch)', summary: 'Installing the v1.89.0 hooks on the maddu source repo itself caught hooks install writing the consumer entrypoint everywhere — nonexistent in a source checkout, so every hook errored silently (fails-open masked it). resolveHookBin now picks the entrypoint per repo layout at install time, and doctor’s stanza-currency check uses the same resolution. Consumers were never affected.' },
  { v: 'v1.89.0', id: 'v1-89-0', date: '2026-07-03', type: 'feature',   headline: 'Pre-compaction governance checkpoint', summary: 'maddu hooks install now also wires Claude Code’s PreCompact hook — manual /compact and auto-compaction both append a COMPACTION_CHECKPOINT to the spine: the trigger, the last recorded slice-stop (the durable anchor — anything after it that wasn’t recorded did not survive), handoff currency, open approvals, active claims. Fails open — always exit 0, because a governance instrument must never break the session it observes. maddu orient auto-announces the latest checkpoint; doctor validates hook-stanza currency.' },
  { v: 'v1.88.0', id: 'v1-88-0', date: '2026-07-03', type: 'hardening', headline: 'Completion-claim gate at every slice-stop', summary: 'A completion-claim gate (warn) flags a live pattern of hedged completion claims ("should work", "seems to pass") on slices with no observed proof — no real GATE_RAN(ok) during the slice, no verified deliverable; self-reported flags deliberately don’t count. Deterministic: a model checking a model is a second opinion; a deterministic check against declared deliverables is evidence. Runs at every slice-stop, surfaces but never blocks, and holds warn tier for at least a quarter of own-repo data before any fail promotion.' },
  { v: 'v1.87.0', id: 'v1-87-0', date: '2026-07-03', type: 'feature',   headline: 'maddu ci — the headless gate rail', summary: 'maddu ci runs every deterministic gate headlessly — no LLM, no network — and exits nonzero only on gates the repo pinned as required (maddu ci pin → the committed ci config). Churn-proof by construction: framework upgrades can add gates without turning anyone’s pipeline red until they re-pin. Local enforcement first — the same exit code drives a fully-offline git pre-push hook; GitHub Actions auto-detection (::error annotations + a job summary table) is optional on top.' },
  { v: 'v1.86.0', id: 'v1-86-0', date: '2026-06-30', type: 'hardening', headline: 'roadmap #14 — cost-budget gate (the runaway-session guard)', summary: 'An opt-in, advisory cost-budget gate. With a repo carrying .maddu/config/cost-budget.json, it sums recent tokenLedger spend over the trailing window and WARNs — never FAILs — when over, so a runaway session shows in doctor/audit without blocking a land. No provider call; it only sums numbers the ledger already holds.' },
  { v: 'v1.85.0', id: 'v1-85-0', date: '2026-06-30', type: 'feature',   headline: 'roadmap #8 — lesson federation (corrections compound across the fleet)', summary: 'maddu learn sync reads sibling repos’ agent-file corrections off the workspace registry and surfaces the portable ones — a lesson recurring in ≥2 repos or tagged @portable. --adopt writes them into this repo’s CLAUDE.md learn block, approval-only, with OS-absolute paths redacted so a sibling’s machine layout never leaks.' },
  { v: 'v1.84.0', id: 'v1-84-0', date: '2026-06-30', type: 'hardening', headline: 'roadmap #13 — compat spine (read an old install safely)', summary: 'Makes reading an old install’s state total so maddu fleet upgrade can deliver into installs back to v1.15 without crashing on a missing key. project() stamps a schemaVersion; normalizeProjection(raw) turns any projection — current, legacy, partial, or garbage — into a total current-shape object; the can-read-old-state gate keeps the reader total.' },
  { v: 'v1.83.0', id: 'v1-83-0', date: '2026-06-30', type: 'feature',   headline: 'roadmap #10-mutation — maddu fleet upgrade --apply (staged delivery)', summary: 'maddu fleet upgrade --apply delivers canonical framework bytes to eligible behind repos by reusing the single-repo maddu upgrade engine. Must be scoped (--only <repo> or --all); each target re-checks quiescence, snapshots the managed bytes (never the spine), upgrades, then runs doctor — the first red halts the whole run and prints a rollback path.' },
  { v: 'v1.82.0', id: 'v1-82-0', date: '2026-06-30', type: 'feature',   headline: 'roadmap #10 — maddu fleet upgrade --plan (the F1 delivery leg)', summary: 'maddu fleet upgrade --plan — a read-only, offline preview of what a fleet delivery would do per behind repo: a quiescence interlock (active claim, dirty tree, or recent spine activity marks a repo blocked, else eligible) and the managed-byte delta vs the recorded manifest hashes. Live, it mapped 12 behind repos → 4 eligible / 8 blocked.' },
  { v: 'v1.81.0', id: 'v1-81-0', date: '2026-06-30', type: 'hardening', headline: 'roadmap #5 — retire the dead skill funnel (F2)', summary: 'Retires the dead skill-candidate funnel (F2): the auto-detector fired but every downstream terminal was 0 across 13 installs, while maddu learn already captures the valuable corrections. The slice-stop auto-trigger is removed, SKILL_CANDIDATE_DETECTED is dispositioned dormant, and a funnel-integrity gate FAILs if it’s ever re-wired.' },
  { v: 'v1.80.0', id: 'v1-80-0', date: '2026-06-30', type: 'hardening', headline: 'roadmap #12 — reposition the charter (F4)', summary: 'Repositions the charter so orchestration-≈0 stops reading as a dead domain (F4). Every verb in _tiers.mjs now carries a layer — core (the always-on session/lane/slice/gate substrate, 62 verbs) vs orchestration (the opt-in multi-agent layer, 4 verbs). maddu audit positioning reports orchestration as an opt-in fire-rate, never as dead.' },
  { v: 'v1.79.0', id: 'v1-79-0', date: '2026-06-30', type: 'hardening', headline: 'roadmap #9 — sharpen the discipline loop', summary: 'Sharpens the core loop: maddu orient opens with a one-glance status line — gates green/red, goal progress, next — and a GATES section renders each hard failure legibly (gate id, the spine event id, the exact maddu doctor --gate <id> repro), never a stack trace. GATE_RAN events now carry the resolved status so a soft warn is never mislabeled a hard fail.' },
  { v: 'v1.78.0', id: 'v1-78-0', date: '2026-06-30', type: 'hardening', headline: 'roadmap #7 — the governance surface is itself budgeted', summary: 'Budgets the governance surface itself. A self-applying cap inside maddu audit gives each governance category — gates, CLI verbs, audit checks — a ceiling read from ground truth; over a cap FAILs unless you retire something or add a waiver row (which raises the ceiling by one and shows as recorded debt).' },
  { v: 'v1.77.0', id: 'v1-77-0', date: '2026-06-30', type: 'hardening', headline: 'audit sprint 3 — the outcome ledger', summary: 'The outcome ledger measures whether the guardrails change an outcome. outcome.mjs counts the faults the gates caught — every FAILED gate run is a recorded PREVENTED_FAULT, split hard vs soft by severity and tallied per gate — and the maddu fleet view now shows per-repo ⚿ N caught and an active-fleet headline.' },
  { v: 'v1.76.0', id: 'v1-76-0', date: '2026-06-30', type: 'feature',   headline: 'audit sprint 2 — Fleet Spine + the self-verifying audit circuit', summary: 'Fleet Spine — maddu fleet, a read-only single-machine aggregator (F1). It walks the workspace registry and digests each repo from its on-disk projection + version.json without running it, tiers them ACTIVE/DORMANT/ABANDONED, and computes each repo’s version delta vs the fleet’s latest. The live fleet immediately showed 12/13 active repos behind.' },
  { v: 'v1.75.0', id: 'v1-75-0', date: '2026-06-30', type: 'hardening', headline: 'audit sprint 1 — structural guardrails from the 13-repo cross-project audit', summary: 'First audit sprint from the 13-repo cross-project audit: three guardrails that each make a fault class structurally impossible. An offline staleness FLOOR nudges maddu upgrade from the install’s own version.json date; a release-parity gate requires a FIXED-IN.json row at the release boundary; a disposition registry drops maddu insights dead count 34 → 0.' },
  { v: 'v1.74.2', id: 'v1-74-2', date: '2026-06-29', type: 'hardening', headline: '.maddu/ runtime state stays out of git — the working tree stops being perpetually dirty', summary: 'maddu init now ships a Policy-A .gitignore that ignores .maddu/* and re-includes only durable authored artifacts (config/skills/plans/wiki, the lane catalog), so runtime spine + projections stop churning the tree. A maddu-state-untracked doctor gate (warn) detects already-tracked rebuildable state and prints the non-destructive git rm --cached remediation.' },
  { v: 'v1.74.1', id: 'v1-74-1', date: '2026-06-29', type: 'patch',     headline: 'Integrity tolerates CRLF — Windows installs stop self-reporting as tampered', summary: 'On a Windows tree with core.autocrlf=true, all ~360 managed files hashed differently and doctor reported a clean install as tampered while upgrade skipped every file. sha256OfFile now collapses CRLF→LF for text before hashing (binary hashed raw), and maddu init writes a Máddu-scoped .gitattributes pinning LF so fresh installs never churn.' },
  { v: 'v1.74.0', id: 'v1-74-0', date: '2026-06-29', type: 'feature',   headline: 'Session discipline by default — fresh repos never build unrecorded', summary: 'A shared resolveSessionId (precedence --session → $MADDU_SESSION_ID → the liveness-verified active-session cache) lets one maddu register flow into lane claim/slice-stop across fresh shells with no flag. New maddu hooks wires Claude Code SessionStart/SessionEnd hooks into .claude/settings.json to auto-register and close a session.' },
  { v: 'v1.73.1', id: 'v1-73-1', date: '2026-06-28', type: 'patch',     headline: 'maddu doctor stops crying wolf on consumer installs', summary: 'The generated-artifacts-current gate could hard-FAIL on a healthy install, pointing at scripts/generate.mjs that doesn’t ship to consumers. It now skips on the positive presence of generate.mjs (the source-checkout marker) instead of inferring consumer-vs-source from target absence, so the message and the demand can no longer disagree.' },
  { v: 'v1.73.0', id: 'v1-73-0', date: '2026-06-25', type: 'patch',     headline: 'maddu debt accuracy — the ledger stops miscounting itself', summary: 'The deliberate-shortcut scanner miscounted itself, even stamping the best-documented marker [no-trigger] because its upgrade: trigger lived on a continuation line. scanDebt now gathers adjacent comment-continuation lines, only counts a token that begins a comment body, and skips doc files — real debt went 21/14/7 → an accurate 2/2/0.' },
  { v: 'v1.72.0', id: 'v1-72-0', date: '2026-06-25', type: 'feature',   headline: 'maddu agents — "install maddu" on demand, machine-wide', summary: 'maddu agents <detect|register|unregister> makes "install maddu" an instruction every agent understands in every repo. It merges a self-contained install stanza into each agent’s global instruction file (Claude Code, Codex, Gemini, generic) via an idempotent marker block that preserves operator content; paths resolve from os.homedir(), never hardcoded.' },
  { v: 'v1.71.0', id: 'v1-71-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — command-bar module (composer + palette)', summary: 'Extracts the slash-command bar (composer + Ctrl-K palette) into cockpit-command-bar.js via a host-injection seam. cockpit.js drops 2423 → 1715 (−81% from the 9202-line monolith) and is now the minimal composition root — the decomposition is complete.' },
  { v: 'v1.70.0', id: 'v1-70-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — Inspector module (optional shell polish)', summary: 'Extracts the Inspector entity-detail drawer into cockpit-inspector.js (open state, ensureInspector, the five tab renderers). cockpit.js 2686 → 2423; the cockpit is now 16 modules. First of the optional post-completion polish slices.' },
  { v: 'v1.69.0', id: 'v1-69-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — BOSS (route-view extraction COMPLETE)', summary: 'Moves the last route view, BOSS (the proposal · enforcer · decision terminal), into cockpit-views-live.js. Every one of the cockpit’s 42 routes now renders from a sibling module; cockpit.js 3115 → 2686 (−71% from the monolith), a pure composition root.' },
  { v: 'v1.68.0', id: 'v1-68-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Conductor)', summary: 'Moves Conductor (Next Command, KPI strip, Now/Next/Waiting/Done board, Operation Score Matrix) into cockpit-views-live.js, and the shared reason-code palettes down to the cockpit-event-rows leaf. cockpit.js 3346 → 3115.' },
  { v: 'v1.67.0', id: 'v1-67-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Workbench)', summary: 'Moves Workbench (the 3-pane operator cockpit) into cockpit-views-live.js behind two new narrow seams ctx.refreshStatus() and ctx.onRouteLeave(). cockpit.js 3613 → 3346; a Codex consult confirmed the final trio is composer-free.' },
  { v: 'v1.66.0', id: 'v1-66-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Chats — last non-composer view)', summary: 'Moves Chats (the sessions roster), the last route view that doesn’t drive the composer, into cockpit-views-live.js with no new ctx seams. cockpit.js 3645 → 3613; only the composer-deep trio remains inline.' },
  { v: 'v1.65.0', id: 'v1-65-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Queue Board + Claim Map)', summary: 'Moves the two scheduler/lane views, Queue Board and Claim Map, into cockpit-views-live.js with zero new ctx seams; their private builders take ctx so card/row clicks reach ctx.openInspector. cockpit.js 3857 → 3645.' },
  { v: 'v1.64.0', id: 'v1-64-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Dashboard)', summary: 'Moves the headline Dashboard overview into cockpit-views-live.js behind a new cached-snapshot seam ctx.bridgeStatus() + ctx.bridgeOk(). cockpit.js 4010 → 3857 — under 4k.' },
  { v: 'v1.63.0', id: 'v1-63-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Orientation + Gates + Reviews)', summary: 'Three read-only ledger views — Orientation, Gates, and Reviews — move into cockpit-views-live.js in one slice with zero new seams. cockpit.js 4236 → 4010.' },
  { v: 'v1.62.0', id: 'v1-62-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Approvals)', summary: 'Moves the Approvals view into cockpit-views-live.js behind a new shared-read seam ctx.fetchApprovals (also called by the still-inline Workbench, so it joins ctx without duplication). cockpit.js 4390 → 4236.' },
  { v: 'v1.61.0', id: 'v1-61-0', date: '2026-06-20', type: 'polish',    headline: 'Cockpit decomposition — live cluster (Events stream view)', summary: 'Moves the live Events stream view into cockpit-views-live.js behind a new stream-pause seam ctx.isStreamPaused() + ctx.toggleStreamPause(); its handler appends each matching row live rather than refetching. cockpit.js 4482 → 4390.' },
  { v: 'v1.60.0', id: 'v1-60-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — live cluster (operations + swarm)', summary: 'Moves Operations (receipt log, hindsight memory, checkpoints) and Swarm into cockpit-views-live.js behind one new shared-read seam ctx.fetchMemory. cockpit.js 4742 → 4482.' },
  { v: 'v1.59.0', id: 'v1-59-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — live cluster begins (mailbox + tasks + skills)', summary: 'Opens cockpit-views-live.js with the first live-cluster trio: Mailbox, Tasks, and Skills, whose only shell couplings are proven ctx seams (ctx.onSpineEvent, ctx.currentSession). cockpit.js 5166 → 4742; now 14 modules.' },
  { v: 'v1.58.0', id: 'v1-58-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — connect cluster complete (schedule + mcp + runtimes)', summary: 'Extracts the last three connect views (Schedule, MCP, Runtimes) into cockpit-views-connect.js behind one new accessor ctx.scopeIsGlobal(route), finishing the connect cluster. cockpit.js 5627 → 5166.' },
  { v: 'v1.57.0', id: 'v1-57-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — Imports view (+ the narrow currentSession accessor)', summary: 'Extracts the Imports view into cockpit-views-connect.js and adds the narrow read-only ctx.currentSession() accessor (the composer’s sticky session pointer) so POST actions can stamp by:. cockpit.js 5759 → 5627.' },
  { v: 'v1.56.0', id: 'v1-56-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — the stream-subscription seam (+ Auth view)', summary: 'Introduces the event-stream subscription seam ctx.onSpineEvent(handler) (subscribe + auto-teardown on route change, with a torn-flag guard) and proves it by extracting the Auth view — which unblocks every remaining stream-coupled view. cockpit.js 5887 → 5759.' },
  { v: 'v1.55.0', id: 'v1-55-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — connect cluster begins (Settings + Trust)', summary: 'Opens the connect cluster with its two stream-free views, Settings and Trust, in a new cockpit-views-connect.js; both extracted programmatically to avoid transcription error on a ~530-line move. cockpit.js 6417 → 5887 — the largest single slice.' },
  { v: 'v1.54.0', id: 'v1-54-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — inspect-heavy: Plans view (cluster complete)', summary: 'The sixth and final inspect-heavy view, Plans (the Now/Next/Blocked/Done kanban board), joins cockpit-views-inspect.js, completing the inspect-heavy cluster; the ctx seam grows to add openEntityDrawer. cockpit.js 6569 → 6417.' },
  { v: 'v1.53.0', id: 'v1-53-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — inspect-heavy: Agents view (+ a narrow router-rerender seam)', summary: 'The Agents coworker/session grid joins cockpit-views-inspect.js, the first scope-aware view; the seam grows scopePill, scopedUrl, and a narrow ctx.rerender alias rather than handing the view the router. cockpit.js 6651 → 6569.' },
  { v: 'v1.52.0', id: 'v1-52-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — inspect-heavy: Roadmap view', summary: 'The Roadmap view (slice-stop KPIs, a 28-day closure-cadence bar, a lane-mix table, the slice index) joins cockpit-views-inspect.js with no ctx growth — its charts build inline. cockpit.js 6810 → 6651.' },
  { v: 'v1.51.0', id: 'v1-51-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — inspect-heavy: Workflows view', summary: 'The Workflows view (the architecture blueprint SVG node/edge graph, each node opening in the Inspector) joins cockpit-views-inspect.js with no ctx growth; its only shell dep, openInspector, is already on the seam. cockpit.js 6930 → 6810.' },
  { v: 'v1.50.0', id: 'v1-50-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — inspect-heavy: Teams view', summary: 'The Teams lane-ownership map joins cockpit-views-inspect.js; the seam grows the two bridge fetch helpers (fetchLanes/fetchProjection) and the palette focus pair so a ?focus=<lane> deep link flashes the card. cockpit.js 7013 → 6930.' },
  { v: 'v1.49.0', id: 'v1-49-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — first inspect-heavy view (Learning)', summary: 'Opens the inspect-heavy cluster with the Learning hindsight-memory browser in a new cockpit-views-inspect.js; the seam grows openInspector, and its fixture fires a row click to assert the interaction headlessly. cockpit.js 7148 → 7013.' },
  { v: 'v1.48.0', id: 'v1-48-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — Docs view extracted', summary: 'Pulls the Docs route renderer (the in-cockpit manual reader — markdown pages, auto-TOC, in-article link interception, a self-removing hashchange listener) into cockpit-views-docs.js; a pure leaf move, no ctx. cockpit.js 7398 → 7148.' },
  { v: 'v1.47.0', id: 'v1-47-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition — reference view cluster extracted', summary: 'Extracts the five reference route renderers (Goal, Tools, Loops, Search, Wiki) into cockpit-views-reference.js; only renderGoal needs a shell helper, injected as ctx.panelFocus. cockpit.js 7779 → 7398.' },
  { v: 'v1.46.0', id: 'v1-46-0', date: '2026-06-19', type: 'polish',    headline: 'Real-browser smoke gate (Playwright) + workbench first-paint fix', summary: 'Adds a real-browser smoke gate, cockpit-playwright.mjs — loads the shipped cockpit in headless Chromium, drives hashchange across all 42 routes, and graceful-skips when Playwright is absent. On day one it caught a TypeError aborting Workbench first-paint (updateTabs reading a count span pre-attach), now null-guarded.' },
  { v: 'v1.45.0', id: 'v1-45-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — ctx seam + first view module', summary: 'Introduces the dependency-injection ctx seam threaded through the router (route.render(ctx)) and extracts the first view module, cockpit-views-backbone.js (the six v0.18 single-panel routes). Gate B stayed byte-identical across all 43 goldens.' },
  { v: 'v1.44.0', id: 'v1-44-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — route-metadata split', summary: 'Splits the route table so view modules can import route metadata without the render graph: a new pure-data cockpit-route-meta.js holds ROUTE_META for all 42 routes, and cockpit.js rebuilds ROUTES by merging renderers onto it. Gate B byte-identical across 43 goldens.' },
  { v: 'v1.43.0', id: 'v1-43-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — small util leaves', summary: 'A batch of small standalone helpers (copyToClipboardWithToast, workspaceBadge, laneFromFact, errorState) join the cockpit-util.js leaf. Gate B stayed byte-identical across all 43 goldens.' },
  { v: 'v1.42.0', id: 'v1-42-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — markdown leaf', summary: 'The Docs route’s markdown renderer moves to a new cockpit-markdown.js leaf — a self-contained CommonMark-ish string→HTML transform with no DOM and no cockpit state. Gate B byte-identical across 43 goldens.' },
  { v: 'v1.41.0', id: 'v1-41-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — event-row leaf', summary: 'The spine-event rendering helpers (classifyEvent, eventRow, prepend, makeDecisionButton) move to a new cockpit-event-rows.js leaf. Gate B stayed byte-identical across all 43 goldens.' },
  { v: 'v1.40.0', id: 'v1-40-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — backbone card leaf', summary: 'The pure data→DOM card renderers of the v0.18 backbone routes move together to a new cockpit-backbone-cards.js leaf (−260 lines). Gate B byte-identical across 43 goldens.' },
  { v: 'v1.39.0', id: 'v1-39-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — loading-skeleton leaf', summary: 'The skeleton-placeholder helpers loading (83 call sites) and loadingFor (the shape-aware variants) move verbatim to the cockpit-util.js leaf. Gate B stayed byte-identical across all 43 goldens — no golden update.' },
  { v: 'v1.38.0', id: 'v1-38-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit decomposition (Phase 1) — formatter leaf', summary: 'The first slice riding the new headless harness: the formatters formatAge, ageTone, formatTs move to the cockpit-util.js leaf — the first cockpit slice in project history verified without an operator browser refresh. Gate B byte-identical across 43 goldens.' },
  { v: 'v1.37.0', id: 'v1-37-0', date: '2026-06-19', type: 'polish',    headline: 'Cockpit verification harness (Phase 0)', summary: 'Makes the cockpit SPA verifiable headlessly: a happy-dom env, Gate A (cockpit-boot renders all 42 routes without a throw) and Gate B (cockpit-snapshot diffs every route’s DOM against 43 committed goldens). The only shipped change is a two-line prod-identical seam, so future slices self-verify like the server slices did.' },
  { v: 'v1.36.1', id: 'v1-36-1', date: '2026-06-18', type: 'polish',    headline: 'Docs sweep — architecture refactor', summary: 'Docs-only patch closing the two doc gaps the v1.19.0→v1.36.0 architecture refactor left behind: 03-cli-reference.md now lists maddu architecture mass, and 00-index.md’s version banner refreshes from a stale v1.12.0 to v1.36.0 / 62 verbs / audit 14/14. No code or behavior change.' },
  { v: 'v1.36.0', id: 'v1-36-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (18) — cockpit split, slice 3 (comms panels + shared toast)', summary: 'The three comms-plugin settings panels move to a new cockpit-comms.js, and showToast becomes a proper cockpit-util.js leaf for reuse (~130 call sites). cockpit.js 8942 → 8555; still the sole monolith.' },
  { v: 'v1.35.0', id: 'v1-35-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (17) — cockpit split, slice 2 (widget kit)', summary: 'The widget kit (statusGrid, bar, segBar, donut, sparkline, meter, binByTime) moves to a new cockpit-widgets.js leaf — pure inline SVG/DOM, no chart lib, no back-reference to cockpit state. cockpit.js 9202 → 8942.' },
  { v: 'v1.34.0', id: 'v1-34-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (16) — server split, slice 10 (collaboration routes)', summary: 'The BOSS / proposal collaboration subsystem (routeProposals, routeBoss) moves to bridge-routes-collab.mjs. server.js 1193 → 1063 — ~61% off the original bridge monolith.' },
  { v: 'v1.33.0', id: 'v1-33-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (15) — server split, slice 9 (work-execution routes)', summary: 'The five work-execution route groups (routeWorkers, routeSkills, routeTasks, routeMailbox, routeMemory) move to bridge-routes-work.mjs. server.js 1434 → 1193 — ~56% off the bridge monolith.' },
  { v: 'v1.32.0', id: 'v1-32-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (14) — server split, slice 8 (capability routes) · server.js is no longer a monolith', summary: 'Four capability route groups (imports/auth/checkpoints/schedules) move to bridge-routes-capabilities.mjs. server.js 1576 → 1434 — dropping below the 1500-line threshold, so it is no longer a monolith and the mass ratchet now tracks only cockpit.js.' },
  { v: 'v1.31.0', id: 'v1-31-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (13) — server split, slice 7 (approval-gateway routes)', summary: 'The approval gateway (routeApprovals, with its per-repo → global auto-decide cascade) moves to bridge-routes-approvals.mjs. server.js 1657 → 1576.' },
  { v: 'v1.30.0', id: 'v1-30-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (12) — server split, slice 6 (lane-ownership route groups)', summary: 'The lane-ownership subsystem (routeSessions, routeLanes — the substrate behind rule #8) moves to bridge-routes-lanes.mjs. server.js 1837 → 1657.' },
  { v: 'v1.29.0', id: 'v1-29-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (11) — server split, slice 5 (registry route groups)', summary: 'The first route-group extraction out of the handleBridge if-chain. The two registry CRUD groups (routeMcp, routeRuntimes) move to bridge-routes-registries.mjs behind a new reusable dispatch contract (route<Group> returns true when it owns the path, false to fall through). server.js 1941 → 1837.' },
  { v: 'v1.28.0', id: 'v1-28-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (10) — server split, slice 4 (bootstrap helpers)', summary: 'The bridge’s pre-listen bootstrap helpers (resolveRepoRoot, detectFrameworkLayout, readVersion, pickPort, probePortIsMaddu, findPidOnPort) move to bridge-bootstrap.mjs. server.js 2035 → 1941.' },
  { v: 'v1.27.0', id: 'v1-27-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (9) — server split, slice 3 (workspace fan-out)', summary: 'The /bridge/_all/* multi-workspace fan-out helpers (fanoutProjection/fanoutConductor/fanoutApprovals/fanoutQueue/fanoutEventsRecent) move to bridge-fanout.mjs. server.js 2237 → 2035 — ~25% off the bridge monolith.' },
  { v: 'v1.26.0', id: 'v1-26-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (8) — server split, slice 2 (projection builders)', summary: 'The four cockpit projection builders (buildConductor, buildQueueBoard, buildClaimMap, buildBacklinks) move to bridge-builders.mjs. A live-bridge boot caught two interspersed-shared-helper bugs instantly — the dividend of splitting the verifiable monolith first. server.js 2630 → 2237.' },
  { v: 'v1.25.0', id: 'v1-25-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (7) — server split, slice 1 (HTTP transport)', summary: 'Begins decomposing the 2705-line server.js bridge monolith along HTTP boundaries, pure helpers first. The seven transport helpers (send, sendJson, readBody, serveStatic, the loopback-hostname guard, …) move to http-util.mjs. server.js 2705 → 2630.' },
  { v: 'v1.24.0', id: 'v1-24-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (6) — cockpit split, slice 1 (prove the pattern)', summary: 'Begins decomposing the 9260-line cockpit.js SPA monolith into browser-native ES modules — no bundler, no build step. The six pure leaf utilities (el, panel, placeholder, formatUptime, compactPath, truncatePathFromLeft) move to a sibling cockpit-util.js. cockpit.js 9260 → 9202.' },
  { v: 'v1.23.0', id: 'v1-23-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (5) — structural mass: the monolith ratchet', summary: 'maddu architecture gains a second dimension: file mass. maddu architecture mass reports per-file line counts and exact-duplicate files, and a shrink-only architecture-mass gate grandfathers cockpit.js (9260) and server.js (2705) into a baseline so they can only get smaller. maddu audit 13 → 14.' },
  { v: 'v1.22.0', id: 'v1-22-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (4) — retire the policing gate (the proof)', summary: 'Retires the docs-in-sync policing gate now that the doc tree is generated (generated-artifacts-current asserts byte-exact, stronger), proving the single-sourcing worked. rule-invariant is repurposed — not deleted — as a substance canary over the generated briefs. maddu audit 14 → 13.' },
  { v: 'v1.21.0', id: 'v1-21-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (3) — the doc tree is generated, not hand-mirrored', summary: 'The bundled docs payload is now generated from the authored docs/ source, not hand-mirrored. A new EOL-preserving mirror generator expands docs/*.md → template/maddu/docs/*.md (48 files) with zero churn; generated-artifacts-current now covers 52 artifacts.' },
  { v: 'v1.20.1', id: 'v1-20-1', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (2b) — the compact rule stanza joins the registry', summary: 'The third brief surface, the compact .section.md rule stanza, is now generated from the same rules.json registry, so all four agent briefs derive their hard-rules text from one source. With that, the rule-invariant drift gate is fully retireable.' },
  { v: 'v1.20.0', id: 'v1-20-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (2) — rule-registry single-sourcing', summary: 'The 8+1 hard rules, previously spelled out by hand in two verbose briefs, are now authored once in rules.json and generated into both via an EOL-aware marker-injection engine. A byte-exact migration — the only change is the two marker lines — so rule-invariant still passes.' },
  { v: 'v1.19.0', id: 'v1-19-0', date: '2026-06-18', type: 'hardening', headline: 'Architecture refactor (1/3) — guardrail · loader-dedup · gen-discipline', summary: 'Three slices that lock in the clean import layering: a 10-module target contract enforced by maddu audit (failOn:new), loader-dedup converging six hand-rolled lib loaders onto two single sources (−91 lines), and a gen-discipline boundary — a deterministic generation engine + a generated-artifacts-current drift gate.' },
  { v: 'v1.18.2', id: 'v1-18-2', date: '2026-06-18', type: 'hardening', headline: 'Capability docs — debt deep-dive, slice-stop guards, verb→doc map', summary: 'Doc-completeness pass matching the post-v1.16 surface: a maddu debt deep-dive (docs/41-debt.md), the v1.17.0 slice-stop change-risk + deliverable guards documented in the canonical ritual doc, and a machine-readable verb→doc map (capability-docs.json) for all 62 verbs. A new capability-docs audit check keeps the map honest — maddu audit 12 → 13.' },
  { v: 'v1.18.1', id: 'v1-18-1', date: '2026-06-18', type: 'patch',     headline: 'Charter coherence — architecture is a capability verb', summary: 'A coherence patch surfaced while mirroring the charter into the marketing site: maddu architecture shipped in v1.18.0 but was never in the charter’s capability-verb table, and charter-drift passed it anyway by matching the prose word "architecture". The verb is added under Quality & review (62 verbs), and charter-drift now requires a backtick-quoted token.' },
  { v: 'v1.18.0', id: 'v1-18-0', date: '2026-06-18', type: 'feature',   headline: 'Architecture drift — maddu architecture (MVP)', summary: 'Declare intended architecture as path-glob modules + allow/forbid rules, extract the real import graph by stdlib regex, and detect drift — forbidden edges (with file:line), cycles, undeclared areas, uncovered files — stamped with a driftScore. A failOn ladder (none/new/any) plus a baseline ratchet gates only new drift; mermaid diagram, ARCHITECTURE_SCANNED event, audit grows 11 → 12.' },
  { v: 'v1.17.0', id: 'v1-17-0', date: '2026-06-18', type: 'hardening', headline: 'Robustness bundle — drift gate, debt ledger, risk + deliverable checks', summary: 'Five self-contained safeguards: a rule-invariant drift gate pins 13 load-bearing phrases across the agent briefs (audit 10 → 11); maddu debt ledgers deliberate shortcuts and flags those with no upgrade trigger; a deterministic risk classifier stamps every slice-stop and escalates review on high/critical changes; deliverable verification flags reported-but-missing files; and routing refuses to act on pasted content.' },
  { v: 'v1.16.0', id: 'v1-16-0', date: '2026-06-18', type: 'feature',   headline: 'Test command split — source self-test + adaptive project tests', summary: 'maddu test splits cleanly: maddu self-test is the framework-source runner (smoke/quick/full profiles, gated by self-test-recent), while maddu test keeps the v1.1 detected-runner wrapper for consumer repos or switches to adaptive discovery profiles. Fleet-audit drift hardening — workspace roles, idempotent lane release, prev_hash-preserving upgrades — finalized here.' },
  { v: 'v1.15.0', id: 'v1-15-0', date: '2026-06-11', type: 'feature',   headline: 'Blueprint robustness (--distill) + README rework', summary: 'The blueprint extractor is hardened and pinned against malformed transcripts, and an optional --distill pass spawns a provider CLI to rewrite the deterministic skeleton into flowing prose (the deterministic export stays canonical). The README is reorganized from version-archaeology into a product-first description of what Máddu is today.' },
  { v: 'v1.14.0', id: 'v1-14-0', date: '2026-06-09', type: 'hardening', headline: 'Spine tamper-evidence — forward prev_hash chain', summary: 'Every event now carries prev_hash — the SHA-256 of the preceding event’s stored NDJSON line — so a consistent rewrite of an interior event is no longer invisible. spine verify recomputes the chain and flags the first broken link (chain_broken) or unchained gap. WARN not FAIL (the no-mutex append path can also fork), forward-only, never auto-repaired. Pure stdlib crypto, no new dependency.' },
  { v: 'v1.13.0', id: 'v1-13-0', date: '2026-06-09', type: 'hardening', headline: 'Robustness hardening — tighten the guarantees', summary: 'A safeguard pass that tightens existing guarantees with no new machinery: append durability + torn-line detection, bridge loopback-origin enforcement (DNS-rebinding defense), blueprint output secret-scanned, determinism tests, extended spine-verify referential coverage, and a hard-rule ↔ gate traceability check. Framework-repo doctor is now self-explanatory.' },
  { v: 'v1.12.0', id: 'v1-12-0', date: '2026-06-09', type: 'feature',   headline: 'Project blueprint — maddu blueprint', summary: 'Exports a portable, variable-driven handoff of how a whole project was built — intake schema, procedure, problems & fixes, iteration hotspots, the real product repo, and acceptance criteria — so it can be reproduced in non-Máddu contexts. Deterministic, secret-scanned, byte-stable.' },
  { v: 'v1.11.0', id: 'v1-11-0', date: '2026-06-09', type: 'hardening', headline: 'Drift-proofing — single-source config seeding + coherence guards', summary: 'All config defaults extracted to one _config-seed.mjs, eliminating seed drift. Two guard gates land: defaults-single-sourced (FAIL) and brief-coherence (WARN) verify neither is ever duplicated. maddu audit grows 6 → 8.' },
  { v: 'v1.10.0', id: 'v1-10-0', date: '2026-06-09', type: 'feature',   headline: 'Invocation-logic pass 2 — light up the dead domains', summary: 'Auto-fires three previously-dead triggers under the gauntlet: slice-stop derives and emits a resume narrative (auto-handoff), skill-candidate detection generalizes to real products, and auto-review spawns after slice-stop with cooldown guards.' },
  { v: 'v1.9.2',  id: 'v1-9-2',  date: '2026-06-09', type: 'patch',     headline: 'Name maddu learn in the worker brief', summary: 'Adds maddu learn digest / run / memory to the installed worker brief so agents discover the capability directly.' },
  { v: 'v1.9.1',  id: 'v1-9-1',  date: '2026-06-09', type: 'patch',     headline: 'maddu learn — Windows judgment-worker spawn fix', summary: 'Routes the judgment prompt via STDIN instead of argv and uses a shell only for bare .cmd shims, fixing a Windows spawn failure.' },
  { v: 'v1.9.0',  id: 'v1-9-0',  date: '2026-06-09', type: 'feature',   headline: 'Failure learning — maddu learn', summary: 'Mines failed→succeeded tool-call pairs from Claude Code transcripts and distils durable project corrections into the brief and .maddu/memory.ndjson, with reversible briefings and supersession chains. Inspired by Headroom.' },
  { v: 'v1.8.0',  id: 'v1-8-0',  date: '2026-06-05', type: 'hardening', headline: 'Rule scope boundary — Máddu’s rules govern Máddu, not your product', summary: 'The 8+1 hard rules are explicitly scoped to the framework layer. Agents building with Máddu can use any SDK, backend, or database for their own product — the rules never constrain the host repo. A scope sweep eradicates residual unscoped rule prose.' },
  { v: 'v1.7.0',  id: 'v1-7-0',  date: '2026-06-04', type: 'feature',   headline: 'Invocation logic — wire WHEN the still-dead domains fire', summary: 'Each still-dormant capability gets a defined trigger condition wired into the flow: trust-audit fires on deps-change, the coordinator checkpoints before runs, and a dormant-by-design registry honestly separates broken from merely-untriggered features.' },
  { v: 'v1.6.0',  id: 'v1-6-0',  date: '2026-06-04', type: 'feature',   headline: 'Orchestration handoff — goal progress + cross-session briefing', summary: 'Goal success conditions become verifiable: maddu orient runs verification commands and shows success-progress (✓/○/?), and maddu handoff set curates a cross-session "▶ RESUME HERE" briefing that persists across sessions.' },
  { v: 'v1.5.0',  id: 'v1-5-0',  date: '2026-06-03', type: 'feature',   headline: 'Real sub-worker spawn + tracking', summary: 'Máddu spawns and tracks real sub-workers across disjoint lanes — the coordinator spawns tracked workers per phase, maddu team spawn fans work concurrently, and OAuth sub-worker tracking enables genuine multi-agent coordination.' },
  { v: 'v1.4.0',  id: 'v1-4-0',  date: '2026-06-03', type: 'feature',   headline: 'Empirical insights, plugin loader, tighter core', summary: 'maddu insights (read-only usage census) and maddu plugin (capabilities outside the core). Comms demoted to the first plugin, off by default. Skills funnel auto-fires at slice-stop; the coordinator enforces review. The first data-driven release.' },
  { v: 'v1.3.0',  id: 'v1-3-0',  date: '2026-05-24', type: 'feature',   headline: 'Coherence realignment + completeness', summary: 'Three default pipelines, maddu audit charter-drift command, forgiving CLI verbs, slash on-ramp triage, charter published. Closed the feature-sprawl diagnosis.' },
  { v: 'v1.2.3',  id: 'v1-2-3',  date: '2026-05-24', type: 'polish',    headline: 'Entity drawer', summary: 'Clickable plans and kanban cards open a right-side detail drawer. Reusable openEntityDrawer primitive for future routes.' },
  { v: 'v1.2.2',  id: 'v1-2-2',  date: '2026-05-24', type: 'polish',    headline: 'Cockpit path polish', summary: 'Compact drive/.../basename path display, click-to-copy with toast, scope-pill active-class fix.' },
  { v: 'v1.2.1',  id: 'v1-2-1',  date: '2026-05-24', type: 'polish',    headline: 'Multi-bridge polish', summary: 'Port-collision refusal, bridges list / kill-all, CWD-vs-workspace triage at start time.' },
  { v: 'v1.2.0',  id: 'v1-2-0',  date: '2026-05-24', type: 'hardening', headline: 'Supply-chain hardening + Hermes adapter', summary: 'maddu trust audit/pin/verify, MCP SHA256 provenance, worker env allowlist default-deny, secret-scan on tool argv, threat-model doc, Trust cockpit route.' },
  { v: 'v1.1.2',  id: 'v1-1-2',  date: '2026-05-23', type: 'patch',     headline: 'Narrative refresh + P3 patches', summary: 'README badge refresh, Python cross-stack detector, skill candidate threshold tuned.' },
  { v: 'v1.1.1',  id: 'v1-1-1',  date: '2026-05-22', type: 'patch',     headline: 'Burn-in patch · 12 fixes', summary: 'Windows spawn EINVAL fix, ralph verify exit-code, plan --plan argv collision, kanban phases aggregation, maddu stop + SIGINT, install validation.' },
  { v: 'v1.1.0',  id: 'v1-1-0',  date: '2026-05-22', type: 'feature',   headline: 'Autonomy + planning + tool gateway', summary: 'Default tools, MCP template gallery, governance tiers, receipt log, kanban + plan persistence + auto-revision, loops, coordinator as Máddu primitive, skills starter pack.' },
  { v: 'v1.0.5',  id: 'v1-0-5',  date: '2026-05-22', type: 'patch',     headline: 'Docs sweep · Phase-X leakage cleared', summary: 'Deleted stale roadmap doc, cleaned Phase markers from four user docs.' },
  { v: 'v1.0.4',  id: 'v1-0-4',  date: '2026-05-22', type: 'patch',     headline: 'Generic default lane catalog', summary: 'Replaced internal lanes with 7 generic ones: architecture, frontend, backend, infra, tests, docs, general.' },
  { v: 'v1.0.3',  id: 'v1-0-3',  date: '2026-05-22', type: 'patch',     headline: 'Framework-only routes hidden on consumer installs', summary: 'frameworkOnly:true flag in ROUTES. Reusable affordance for framework-internal surfaces.' },
  { v: 'v1.0.2',  id: 'v1-0-2',  date: '2026-05-22', type: 'patch',     headline: 'Stage-scroll hotfix', summary: 'CSS grid + overflow fix: min-height: 0 on .stage + .stage-body. Composer no longer dragged below viewport.' },
  { v: 'v1.0.1',  id: 'v1-0-1',  date: '2026-05-22', type: 'patch',     headline: 'Cockpit UX patch', summary: 'Anchor composer, collapse sidenav, recent group with localStorage persistence.' },
  { v: 'v1.0.0',  id: 'v1-0-0',  date: '2026-05-22', type: 'milestone', headline: 'Stable declaration', summary: 'Public-surface stability commit. The 8+1 hard rules, the spine NDJSON format, the 13 slash command names, the cockpit routes, the CLI surface, the marker discipline are stable from here.' },
  { v: 'v0.19.2', id: 'v0-19-2', date: '2026-05-21', type: 'polish',    headline: 'UX polish · 6 cockpit routes', summary: 'Slash output re-print pattern, 6 dedicated cockpit routes: Cost, Advisors, Skill Injections, Model Routing, Test Status, Pipelines.' },
  { v: 'v0.19.1', id: 'v0-19-1', date: '2026-05-21', type: 'patch',     headline: 'Burn-in patch · 11 fixes', summary: 'Slash command marker regression, display-vs-summarize discipline, MADDU_SESSION_ID env honoring, slice-stop positional summary, /maddu-suggest (13th slash).' },
  { v: 'v0.19.0', id: 'v0-19-0', date: '2026-05-21', type: 'feature',   headline: 'Completeness + hardening · Rule #9 becomes permanent', summary: 'Token ledger emission via worker wrappers, advisor subprocess spawn, skill auto-injection, model routing hints, stress harness, upgrade matrix, 7 new doctor gates. Rule #9 graduates from candidate to permanent + enforced.' },
  { v: 'v0.18.0', id: 'v0-18-0', date: '2026-05-21', type: 'feature',   headline: 'OMC-inspired UX shell', summary: '12 slash commands as the operator on-ramp, intent routing wired in agent files, maddu suggest engine, multi-runtime agnostic surface.' },
  { v: 'v0.17.1', id: 'v0-17-1', date: '2026-05-20', type: 'milestone', headline: 'Arc starting point', summary: '18 doctor gates PASS, baseline before the v1.0 push. Every release after this had to keep doctor green and layout-refusal at 4/4.' },
];
